# API

```@meta
CurrentModule = ModularSystems
```

Every exported symbol appears here. `checkdocs = :exports` in `docs/make.jl` fails the build if one
is missing, so this page cannot silently fall behind the code.

```@autodocs
Modules = [ModularSystems]
```
