var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"Every exported symbol appears here. checkdocs = :exports in docs/make.jl fails the build if one is missing, so this page cannot silently fall behind the code.","title":"API"},{"category":"module","location":"api/#ModularSystems.ModularSystems","page":"API","text":"ModularSystems\n\nEconomic models assembled from composable blocks of constraints, solved as square systems or as optimization problems.\n\nA Block is a collection of constraints over a JuMP model, each optionally paired with the variable it determines, together with the variables being solved for and an optional objective. Blocks compose with +; swap exchanges which variable an equation is understood to determine, which is how calibration reuses the behavioural equations unchanged. Data lives in a Dataset, and solve applies one to the other.\n\nBuilt on JuMP: a model's state is a JuMP.Model, and blocks are a layer over its variables and constraints. See docs/src/design.md for the decisions behind this and notes/TODO.md for what is still open.\n\n\n\n\n\n","title":"ModularSystems.ModularSystems"},{"category":"type","location":"api/#ModularSystems.BindingBoundError","page":"API","text":"BindingBoundError\n\nRaised when a bound is active at the solution of a square system. The system then did not determine the answer — the problem has silently become a complementarity problem, and the solver still reports success. On a block with an objective an active bound is normal and is not raised.\n\nvariables lists the offending variables paired with the bound they sit on.\n\n\n\n\n\n","title":"ModularSystems.BindingBoundError"},{"category":"type","location":"api/#ModularSystems.Block","page":"API","text":"Block(model; square = false)\n\nA collection of constraints over a JuMP model, with the variables being solved for and an optional objective. Blocks are built one at a time and combined.\n\nSquareness is a predicate, not an invariant: a block need not pair every constraint with a variable, and a block carrying an objective is an optimization problem rather than a square system. Pass square = true to assert that this block is square, which validate then checks — that recovers a construction-time error for blocks that want one, without forcing it on blocks that do not.\n\nUnknowns are derived from the pairings when every solved constraint is paired, and must be declared with set_unknowns! otherwise. Requiring the declaration is what turns a dropped pairing into a degrees-of-freedom mismatch rather than a silent change of problem.\n\n\n\n\n\n","title":"ModularSystems.Block"},{"category":"type","location":"api/#ModularSystems.BlockTriangular","page":"API","text":"BlockTriangular()\n\nSolve the block one subsystem at a time, in the order decompose finds, each subsystem reading the results of the ones before it out of the dataset.\n\nwarning: This is a convergence fallback, not a speed optimisation\nIt is slower than Monolithic — measured at 2× to 110× slower across model sizes from 125 to 21,500 unknowns. Entering an interior-point solver has a setup cost independent of problem size, and a one- or two-variable subsystem cannot amortise it: Ipopt's summed solve time over 400 tiny subsystems was 0.99 s against 0.004 s for the same system solved at once.What it buys is convergence on short systems. Each subsystem starts from the results of the ones before it, where a monolithic solve starts from whatever was supplied for everything at once. Over 70 combinations of shape and starting point at 25–50 periods, this converged 62 times against 58.\n\ndanger: Error accumulates along a long recursive chain\nEach subsystem is solved to an absolute tolerance and its answer is handed on as exact data, with nothing downstream able to correct it. Where a path passes near zero that absolute residual is a large relative error, and it is carried for the rest of the chain. Measured on a 700-period recursive model, the worst residual over the whole system was 3.2e-10 here against 1.2e-13 monolithic — both acceptable, but 2,700× apart — and at 1,000 periods the accumulated error made a subsystem genuinely infeasible while the monolithic solve converged.So: reach for this on a short system that will not converge. Do not reach for it on a long recursive one.\n\nTables and the diagnosis behind both boxes are in archive/decompositionMeasurements.md.\n\nRequires a structurally sound square system. A block with an objective, one whose decomposition has an over- or under-determined part, or one carrying a solved inequality — which determines nothing and so belongs to no subsystem — raises rather than solving something adjacent to what was asked.\n\n\n\n\n\n","title":"ModularSystems.BlockTriangular"},{"category":"type","location":"api/#ModularSystems.CheckFailure","page":"API","text":"CheckFailure\n\nRaised when a @check constraint does not hold at the solution. failures pairs each failing constraint's message with the gap by which it missed.\n\n\n\n\n\n","title":"ModularSystems.CheckFailure"},{"category":"type","location":"api/#ModularSystems.Constraint","page":"API","text":"Constraint\n\nOne constraint of a Block: a JuMP function and set, the variable it is paired with if any, and its role.\n\nThe function and set are held as an unregistered JuMP.AbstractConstraint — built with JuMP.@build_constraint, never added to the model — so a block stays a cheap value that can be copied and recombined, and nothing reaches MOI until a solve builds the intermediate model.\n\nsource records where a @block entry was written. It is what makes an unpaired constraint identifiable in a diagnosis or a failed check, since there is no variable name to call it by.\n\ndetermines is documentation and a square-solver precondition. It names the constraint in solver output and diagnostics and is what squareness is checked against; it does not order the system or drive the solution write-back.\n\n\n\n\n\n","title":"ModularSystems.Constraint"},{"category":"type","location":"api/#ModularSystems.ConstraintRole","page":"API","text":"ConstraintRole\n\nWhether a constraint enters the system being solved (SOLVED) or is evaluated after a solve (CHECKED). Independent of whether the constraint is paired with a variable.\n\n\n\n\n\n","title":"ModularSystems.ConstraintRole"},{"category":"type","location":"api/#ModularSystems.ConvergenceFailure","page":"API","text":"ConvergenceFailure\n\nRaised when fixed_point! runs out of passes. Carries the ConvergenceReport, so the history that shows how it failed is available to the caller.\n\nThis raises rather than returning an unconverged answer for the same reason a binding bound on a square solve raises: every individual solve in a diverging soft link succeeds, so there is no other symptom. A loop that quietly returns its last iterate hands back numbers that look like a solution and are not.\n\nwarning: Warning\nThe datasets hold the last iterate when this is raised. Those numbers are not a solution and must not be used as one.\n\n\n\n\n\n","title":"ModularSystems.ConvergenceFailure"},{"category":"type","location":"api/#ModularSystems.ConvergenceReport","page":"API","text":"ConvergenceReport\n\nWhat an iteration to a fixed point did: whether it converged, how many passes it took, and how far the exchanged quantities were still moving.\n\nchange and every entry of history are measured in multiples of the tolerance, not in the units of any variable: the worst, over the exchanged cells, of |new - old| / max(atol, rtol*|new|). So 1.0 is exactly at tolerance, converged means change <= 1, and one number is comparable across cells of wildly different magnitude and across runs with different tolerances. A dimensional worst-gap would be dominated by the largest cell and would say nothing about the others.\n\nhistory is the useful part when something goes wrong: falling and then flattening means the tolerance is too tight.\n\nscale records the largest absolute value among the exchanged cells at each pass. It exists because history alone cannot tell divergence from stagnation — a link that grows geometrically has a roughly constant change in tolerance units, since the tolerance grows with the value. Whether the numbers themselves are running away is a question only the raw magnitudes answer.\n\n\n\n\n\n","title":"ModularSystems.ConvergenceReport"},{"category":"type","location":"api/#ModularSystems.Dataset","page":"API","text":"Dataset{T}(model)\nDataset(model)          # T = Float64\n\nValues for one scenario over a JuMP model, plus the problem bounds and solve metadata that belong with it. Layers share one key space — the variable slot — so a bound and a value for the same variable are found the same way.\n\nlayer storage why\nvalues dense vector nearly every variable has one\nlower, upper sparse dicts bounds are genuinely rare\nmetadata one record per dataset, not per variable\n\nBounds here are problem bounds, meaningful only for a variable a block treats as unknown. Bounds intrinsic to a variable (K ≥ 0) belong on the JuMP variable instead.\n\nModel metadata is shared through ModelLayout, so scenarios over one model are cheap: copy duplicates the layers, never the model information.\n\nArithmetic (+, -, *, /, and their broadcast forms) operates on values and returns a value-only dataset: bounds and solve metadata are dropped, since a ratio of two scenarios is not itself a scenario. copy is what preserves every layer. A cell missing from either operand is missing from the result rather than being read as zero.\n\n\n\n\n\n","title":"ModularSystems.Dataset"},{"category":"type","location":"api/#ModularSystems.Decomposition","page":"API","text":"Decomposition\n\nWhat decompose found: the block split into subsystems, in an order where each one's inputs are determined by the ones before it.\n\nsubsystems — the well-determined part, in solve order. Every entry is square.\noverdetermined — equations with no variable left to determine, and the variables they share. Non-empty means the block asks more of those variables than they can satisfy.\nunderdetermined — variables no equation is left to determine, with the equations that could have determined them. Non-empty means the block does not pin them down.\n\nA block is solvable subsystem by subsystem exactly when both deficient parts are empty, which is what iswelldetermined asks. This is a structural statement: it says the incidence pattern admits a solution order, not that the numbers do. A structurally sound system can still be numerically singular.\n\n\n\n\n\n","title":"ModularSystems.Decomposition"},{"category":"type","location":"api/#ModularSystems.Diagnosis","page":"API","text":"Diagnosis\n\nWhat diagnose found: the shape of the system, and the structural problems that would otherwise surface as an unexplained solver failure.\n\ntrivial lists solved constraints containing no unknown — after exogenous values are substituted they become a constant equation, which is either always true or always false. orphans lists unknowns appearing in no solved constraint and in no objective, so nothing determines them. missing_values lists exogenous variables with no value in the dataset, which is what the solve would raise on first.\n\nThe remaining three come from decompose and are what counting cannot see. contested lists equations left with no variable to determine, because the variables they could have determined are spoken for; undetermined lists unknowns that do appear in equations but that nothing is left to determine. A block can have zero degrees of freedom and still have both, which is the case a count of unknowns against equations cannot distinguish from a sound system. largest is the size of the biggest simultaneous subsystem, and says how much BlockTriangular would save.\n\nAll three are empty for a block carrying an objective, where there is no pairing to decompose.\n\n\n\n\n\n","title":"ModularSystems.Diagnosis"},{"category":"type","location":"api/#ModularSystems.IndexSet","page":"API","text":"IndexSet(axes::NTuple{N,Symbol}, coords)\nIndexSet(axes, coords)\n\nAn ordered, duplicate-free set of coordinate tuples with named axes. Where a VariableGroup is a set of variable cells, an IndexSet is a set of the coordinates those cells sit at, and it carries the same ∪, ∩ and setdiff.\n\nIts purpose is sparse variables. Declaring one with a filter makes JuMP evaluate the predicate over the full product of the axes, which scales with that product rather than with the number of variables created. Declaring over a coordinate axis does not:\n\npairs = IndexSet((:p, :i), [(:food, :agri), (:steel, :mfg)])\n@variable(model, use[pairs, t in years])     # plain JuMP, no scan\nuse[(:food, :agri), 2030]\n\nA coordinate-keyed container is also dense over its own axes, so there are no absent cells to raise KeyError and nothing for a zero sentinel to paper over. Equations iterate the pattern instead of guarding a product:\n\nsum(use[k, t] * price[k, t] for k in patternA ∩ patternB)\n\nUse select_axes to project onto fewer axes and group_by to bucket coordinates by some of them, which is what replaces summing over one index of a sparse variable.\n\n\n\n\n\n","title":"ModularSystems.IndexSet"},{"category":"type","location":"api/#ModularSystems.ModelLayout","page":"API","text":"ModelLayout\n\nMetadata shared by every Dataset built over one JuMP model. One layout per model, held in model.ext and reached with layout; datasets hold a reference to it rather than a copy, so model-level information is stored once however many scenarios exist.\n\nnslots is a high-water mark of the highest slot seen, not a variable count. It only ever grows.\n\n\n\n\n\n","title":"ModularSystems.ModelLayout"},{"category":"type","location":"api/#ModularSystems.ModelSpec","page":"API","text":"ModelSpec(model)\n\nThe named states a model can be solved in: a registry of Problems over one JuMP model.\n\nModes are kept in registration order, so listing them is deterministic and reads like the workflow they describe. Every mode must be over the same model as the spec, which is the one thing that cannot be checked later.\n\nThe registry is a value like everything else here: registering returns the spec, modes are looked up by name, and nothing anywhere holds a \"current mode\". A mode you are working with is a variable in your own code, not hidden state in the model.\n\nspec = ModelSpec(model)\nregister!(spec, :calibration, Problem(calibration_block, observed))\nregister!(spec, :baseline,    Problem(behavioural, calibrated))\nregister!(spec, :shock,       Problem(spec[:baseline]; data = shocked))\n\nmodes(spec)              # [:calibration, :baseline, :shock]\nsolve!(spec, :baseline)  # solve one of them\ndiagnose(spec)           # check all of them at once\n\n\n\n\n\n","title":"ModularSystems.ModelSpec"},{"category":"type","location":"api/#ModularSystems.Monolithic","page":"API","text":"Monolithic()\n\nSolve every unknown at once in a single intermediate model. The default, and the only strategy that applies to a block carrying an objective.\n\n\n\n\n\n","title":"ModularSystems.Monolithic"},{"category":"type","location":"api/#ModularSystems.Problem","page":"API","text":"Problem(block, data; start = nothing, options = SolveOptions(), strategy = Monolithic())\nProblem(base::Problem; kwargs...)\n\nA block, the dataset it is solved against, where it starts from and how it is solved — one named configuration of a model, checked at construction.\n\nThe constructor runs assert_solvable on the block, so a Problem that exists is one whose structure has been checked. The check is on the block only: a block is a value and cannot change underneath the problem, whereas data is mutable by design — a scenario is a dataset that gets edited — so a data-dependent guarantee made here would be stale by the time it mattered. Missing data is caught at solve time instead, where it is still exact.\n\nThe second form derives a problem from an existing one, changing only the fields named, which is how a shock is expressed as a baseline with something different about it:\n\nshock = Problem(baseline; data = edited, start = baseline_solution)\n\nFields\n\nRead them directly — p.block, p.data, p.start, p.options, p.strategy. There are no exported accessors: data and options are names a user is very likely to want for their own variables, and an exported function makes that assignment an error rather than a shadow. See notes/crossCuttingFindings.md #2.\n\ndata is shared, not copied. A scenario is usually a dataset something else has already written into, and copying here would break that chain silently.\n\nExamples\n\njulia> using JuMP\n\njulia> model = Model();\n\njulia> @variable(model, x); @variable(model, a);\n\njulia> b = @block model begin\n           @square\n           x, x == 2 * a\n       end;\n\njulia> data = Dataset(model); data[a] = 3.0;\n\njulia> p = Problem(b, data)\nProblem(1 constraint, 1 unknown)\n\n\n\n\n\n","title":"ModularSystems.Problem"},{"category":"type","location":"api/#ModularSystems.SolveMetadata","page":"API","text":"SolveMetadata\n\nWhat a solve reports about itself, held per dataset. Every field is empty until a solve writes it.\n\nbinding_bounds lists the unknowns sitting on a bound at the solution, paired with the bound. On a square system that is an error and solve raises; on a block with an objective it is normal, so it is recorded here instead of interrupting.\n\n\n\n\n\n","title":"ModularSystems.SolveMetadata"},{"category":"type","location":"api/#ModularSystems.SolveOptions","page":"API","text":"SolveOptions(; kwargs...)\nSolveOptions(base::SolveOptions; kwargs...)\n\nHow a solve is run, as a value that can be named, reused and overridden. The second form derives options from existing ones, changing only the fields named.\n\nSettings only: SolveOptions carries nothing that belongs to a particular model, so one set of options is reusable across models and scenarios. Starting values are a Dataset and therefore data, which is why they are not here.\n\nFields\n\noptimizer: optimizer factory for this solve. nothing falls back to the one attached to the model with set_optimizer_factory!. Naming it here is what lets a calibration use a different solver, or different solver attributes, from the baseline it feeds.\nreplace_nothing: starting point for an unknown with no value anywhere, or nothing to leave it to the solver.\ncheck_binding_bounds: on a square system, raise BindingBoundError if a bound is active at the solution.\nbound_tolerance: how close to a bound counts as sitting on it. Must not be tighter than the solver's own bound relaxation; see notes/crossCuttingFindings.md #1.\nsilent: suppress solver output.\nrun_checks, check_atol, check_rtol: evaluate @check constraints against the solution.\n\nThe solve strategy is deliberately not here. Every field above is a knob that means the same thing on any model; SolveStrategy selects an algorithm with preconditions, so a profile carrying one would fail on some of the models it was meant to be reusable across. It belongs to the problem, and lives on Problem or as a keyword of solve.\n\nExamples\n\njulia> base = SolveOptions(replace_nothing = 1.0);\n\njulia> loud = SolveOptions(base; silent = false);\n\njulia> loud.replace_nothing, loud.silent\n(1.0, false)\n\n\n\n\n\n","title":"ModularSystems.SolveOptions"},{"category":"type","location":"api/#ModularSystems.SolveStrategy","page":"API","text":"SolveStrategy\n\nHow a block is handed to the solver. Both strategies compute the same answer; they differ in the work done to reach it and in how readily they converge.\n\nSubtypes are Monolithic and BlockTriangular.\n\n\n\n\n\n","title":"ModularSystems.SolveStrategy"},{"category":"type","location":"api/#ModularSystems.StructuralError","page":"API","text":"StructuralError\n\nRaised when a block cannot be solved for structural reasons — before any data, any solver and any numbers. Carries the Diagnosis that found it, so the caller can inspect exactly what was wrong rather than parse a message.\n\n\n\n\n\n","title":"ModularSystems.StructuralError"},{"category":"type","location":"api/#ModularSystems.SubSystem","page":"API","text":"SubSystem\n\nOne group of equations that must be solved together, with the variables they determine.\n\nconstraints indexes into the block's own constraints vector, so a subsystem can be read back against the block it came from. variables are the cells that subsystem solves for.\n\n\n\n\n\n","title":"ModularSystems.SubSystem"},{"category":"type","location":"api/#ModularSystems.SubSystemFailure","page":"API","text":"SubSystemFailure\n\nRaised when a subsystem fails during a BlockTriangular solve. Names the position in the solve order and the variables that subsystem determines, which a bare solver infeasibility does not: on this path the failing subsystem is usually small, and knowing which one it is, is most of the diagnosis.\n\n\n\n\n\n","title":"ModularSystems.SubSystemFailure"},{"category":"type","location":"api/#ModularSystems.Tag","page":"API","text":"Tag(name::Symbol)\n\nA marker for categorising variables — \"these are real quantities\", \"these are growth-adjusted\". Tags are attached with tag! and turned back into a VariableGroup with tagged.\n\nDefine them as constants so a typo is an UndefVarError rather than a silently empty group:\n\nconst GrowthAdjusted = Tag(:growth_adjusted)\n\n\n\n\n\n","title":"ModularSystems.Tag"},{"category":"type","location":"api/#ModularSystems.VariableGroup","page":"API","text":"VariableGroup(items...)\nVariableGroup(model, items...)\n\nAn ordered, duplicate-free collection of variable cells. items may be variable references, containers of them (arrays, JuMP containers), other groups, or any iterable mixing those; they are flattened in the order given.\n\nGroups are what the interface takes wherever a set of variables is meant — declaring unknowns, setting bounds, fixing and freeing. Elements are cells (μ[1], K[2030]), not containers, because every one of those call sites selects cells.\n\nA group is a value frozen at construction, not a query re-evaluated against the model: membership that could change between declaring unknowns and solving would be a wrong answer rather than an error. All members must belong to one model, checked here.\n\nPass model explicitly when the group may be empty; otherwise the model is taken from the members.\n\nSupports ∪, ∩, setdiff, in, length, iterate and getindex. It is deliberately not an AbstractVector — that would invite push!, similar and array broadcasting, none of which a frozen set of cells should promise.\n\n\n\n\n\n","title":"ModularSystems.VariableGroup"},{"category":"method","location":"api/#Base.:+-Tuple{Block, Block}","page":"API","text":"a::Block + b::Block -> Block\n\nCompose two blocks over the same model. Constraints concatenate and pairings must stay distinct, so a variable determined in both blocks is an error rather than a silently dropped equation.\n\nFor many blocks use compose, which does it in one pass. + is non-mutating, so folding it over n blocks is quadratic in the number of blocks.\n\nObjectives. At most one across a sum. Two objectives raise rather than being added together: summing objectives contributed by different modules is a wrong answer with no symptom.\nUnknowns. Unioned. If neither block declares a set, the result declares none either and keeps deriving its unknowns from its pairings.\nSquareness. The result is never marked square, whatever its parts claimed. Two square blocks can each be square alone and not compose to a square system, so inheriting the claim would skip the check exactly where it is most likely to catch something. Re-assert with assert_square!.\n\n\n\n\n\n","title":"Base.:+"},{"category":"method","location":"api/#ModularSystems.ModularSystemsVersion-Tuple{}","page":"API","text":"ModularSystemsVersion() -> VersionNumber\n\nThe version recorded in Project.toml, read from the file rather than baked in, so it cannot drift from what the package manager sees.\n\nExamples\n\njulia> ModularSystemsVersion()\nv\"0.1.0-DEV\"\n\n\n\n\n\n","title":"ModularSystems.ModularSystemsVersion"},{"category":"method","location":"api/#ModularSystems._parse_head-Tuple{Any}","page":"API","text":"_parse_head(head) -> (name, specs, positions, filter)\n\nNormalise the head of a block entry into the variable it pairs with (nothing when unpaired), the index specifications to loop over as (name, set) pairs, the full index list in source order, and an optional filter.\n\npositions and specs differ because an index need not be a loop: K[t0] and x[s in S, :Equity] fix a position rather than range over it. A fixed position appears in positions and not in specs, so it indexes the variable without generating a loop.\n\nJulia parses these five ways depending on whether a filter is present and how many indices there are — ref, vect, typed_vcat, vcat, and a parameters node for the filter — so every form is normalised here rather than in the callers.\n\n\n\n\n\n","title":"ModularSystems._parse_head"},{"category":"function","location":"api/#ModularSystems.add_check!","page":"API","text":"add_check!(b::Block, con, message = \"\") -> Block\n\nAdd a constraint that must hold but determines nothing. Checks never enter the system being solved; solve evaluates them against the solution and raises CheckFailure if one fails.\n\nA check is not a special form — it is a constraint whose role is CHECKED — so it may be an equality or an inequality, and it is never paired with a variable.\n\n\n\n\n\n","title":"ModularSystems.add_check!"},{"category":"method","location":"api/#ModularSystems.add_constraint!-Tuple{Block, JuMP.AbstractConstraint}","page":"API","text":"add_constraint!(b::Block, con; determines = nothing) -> Block\n\nAdd a solved constraint, optionally paired with the variable it determines. Build con with JuMP.@build_constraint, which produces a function and set without touching the model.\n\nRaises if determines names a variable another constraint in the block already determines, or if it is given for an inequality — an inequality determines nothing.\n\nb = Block(model)\nadd_constraint!(b, @build_constraint(x == a + b), determines = x)\n\n\n\n\n\n","title":"ModularSystems.add_constraint!"},{"category":"method","location":"api/#ModularSystems.assert_checks-Tuple{Block, Dataset}","page":"API","text":"assert_checks(b::Block, d::Dataset; atol = 1e-6, rtol = 1e-8) -> Dataset\n\nEvaluate every @check in a block against a dataset, raising CheckFailure if any misses by more than max(atol, rtol * scale). Called by solve; call it directly to test a dataset that was loaded or edited rather than solved.\n\n\n\n\n\n","title":"ModularSystems.assert_checks"},{"category":"function","location":"api/#ModularSystems.assert_solvable","page":"API","text":"assert_solvable(b::Block) -> Block\nassert_solvable(b::Block, d::Dataset) -> Block\n\nRaise StructuralError unless the block is structurally sound: no constraint left with nothing to determine, no unknown left undetermined, no orphan, and no constraint that has become a constant. With a dataset, also check every exogenous variable has a value.\n\nThis is the assertion form of diagnose, which deliberately reports rather than raises. Both exist because they are for different moments: diagnose is for reading while a model is being built, assert_solvable for the point where a configuration is declared fit to solve.\n\nIt is a structural guarantee and not a numerical one. A block that passes can still be singular at the point the solver reaches, or fail to converge.\n\n\n\n\n\n","title":"ModularSystems.assert_solvable"},{"category":"method","location":"api/#ModularSystems.assert_solvable-Tuple{ModelSpec}","page":"API","text":"assert_solvable(spec::ModelSpec) -> ModelSpec\n\nRaise StructuralError on the first mode that is not structurally sound, naming it. Naming it is the whole point: a diagnosis on its own does not say which of a dozen modes it came from.\n\n\n\n\n\n","title":"ModularSystems.assert_solvable"},{"category":"method","location":"api/#ModularSystems.assert_square!-Tuple{Block}","page":"API","text":"assert_square!(b::Block) -> Block\n\nMark a block square and check it immediately. This is how a composed block re-asserts squareness, which + deliberately does not carry over.\n\n\n\n\n\n","title":"ModularSystems.assert_square!"},{"category":"method","location":"api/#ModularSystems.bounds-Tuple{Dataset, JuMP.AbstractVariableRef}","page":"API","text":"bounds(d::Dataset, v) -> (lower, upper)\n\nThe problem bounds on a variable, each nothing when unset. These are the dataset's bounds only; the effective bound at solve time is their intersection with the variable's intrinsic JuMP bounds.\n\n\n\n\n\n","title":"ModularSystems.bounds"},{"category":"method","location":"api/#ModularSystems.clear_bounds!-Tuple{Dataset, JuMP.AbstractVariableRef}","page":"API","text":"clear_bounds!(d::Dataset, v)\n\nRemove both problem bounds from a variable. Intrinsic JuMP bounds are untouched.\n\n\n\n\n\n","title":"ModularSystems.clear_bounds!"},{"category":"method","location":"api/#ModularSystems.clear_bounds!-Tuple{Dataset, VariableGroup}","page":"API","text":"clear_bounds!(d::Dataset, g::VariableGroup)\n\nRemove problem bounds from every variable in a group.\n\n\n\n\n\n","title":"ModularSystems.clear_bounds!"},{"category":"method","location":"api/#ModularSystems.compose-Tuple{Any}","page":"API","text":"compose(blocks) -> Block\n\nCombine many blocks in one pass, with the same rules as +.\n\nUse this rather than sum or a fold over + when there are many. + is non-mutating, so it copies both constraint vectors every time, and folding it over n blocks copies O(n²) constraints — measured at 0.32 s for 2,000 two-equation blocks, against 1.25 ms here. That matters because building a model out of many small blocks, one per period or per sector, is the idiom this package is for.\n\nsum over a Vector or Tuple of blocks calls this. sum over a generator cannot, and falls back to the quadratic fold, so collect it first or call compose directly.\n\n\n\n\n\n","title":"ModularSystems.compose"},{"category":"method","location":"api/#ModularSystems.decompose-Tuple{Block}","page":"API","text":"decompose(b::Block) -> Decomposition\n\nSplit a block into the smallest subsystems that must be solved simultaneously, ordered so each one's inputs are determined by the ones before it, and name whatever is over- or under-determined.\n\nOnly solved equality constraints and the block's unknowns take part: an inequality determines nothing, a check is evaluated after the fact, and an exogenous variable is a number by the time the solver sees it. A block carrying an objective has no pairing to decompose and raises.\n\nThe declared pairings seed the matching, so a well-formed square block costs one pass over the constraints and no search. Where they are missing or do not cover the unknowns a maximum matching is computed instead, which is what lets a block that is not square still be reported on usefully.\n\nExamples\n\njulia> using JuMP\n\njulia> model = Model();\n\njulia> @variable(model, x); @variable(model, y); @variable(model, z);\n\njulia> b = @block model begin\n           @square\n           x, x == 1\n           z, z == y + x\n           y, y == x + 1\n       end;\n\njulia> d = decompose(b)\nDecomposition(3 subsystems, largest 1)\n\njulia> [JuMP.name(only(s.variables)) for s in subsystems(d)]\n3-element Vector{String}:\n \"x\"\n \"y\"\n \"z\"\n\njulia> iswelldetermined(d)\ntrue\n\n\n\n\n\n","title":"ModularSystems.decompose"},{"category":"method","location":"api/#ModularSystems.degrees_of_freedom-Tuple{Block}","page":"API","text":"degrees_of_freedom(b::Block) -> Int\n\nUnknowns minus solved equality constraints. Zero for a square system; the number that generalises to a block with an objective, where squareness does not apply.\n\n\n\n\n\n","title":"ModularSystems.degrees_of_freedom"},{"category":"method","location":"api/#ModularSystems.describe!-Tuple{JuMP.AbstractModel, JuMP.VariableRef, AbstractString}","page":"API","text":"describe!(model, v, text) -> String\n\nGive a variable cell a human-readable description, for table and plot labels. Same store as tags, and likewise attached after declaration.\n\n\n\n\n\n","title":"ModularSystems.describe!"},{"category":"method","location":"api/#ModularSystems.description-Tuple{JuMP.VariableRef}","page":"API","text":"description(v::VariableRef) -> String\n\nA variable's description, or \"\" if it has none.\n\n\n\n\n\n","title":"ModularSystems.description"},{"category":"function","location":"api/#ModularSystems.diagnose","page":"API","text":"diagnose(b::Block, d::Dataset) -> Diagnosis\ndiagnose(b::Block) -> Diagnosis\n\nReport the shape of a block and the structural problems in it, without running a solver.\n\nWithout a dataset, everything is reported except missing_values, which is the one check that needs data. That form is what assert_solvable uses, since a block's structure is a property of the block alone and cannot go stale the way a dataset can.\n\nReports rather than raises: a diagnosis is something to read while building a model. The shape — unknowns, equalities, inequalities, objective, degrees of freedom — is defined whether or not the system is square, which is why it is the useful summary for both kinds of block.\n\nAn orphan means something different on each path. In a square system an unknown in no constraint is a bug. With an objective it may be legitimate, so a variable appearing in the objective is not reported.\n\n\n\n\n\n","title":"ModularSystems.diagnose"},{"category":"method","location":"api/#ModularSystems.diagnose-Tuple{ModelSpec}","page":"API","text":"diagnose(spec::ModelSpec) -> Vector{Pair{Symbol,Diagnosis}}\n\nDiagnose every mode against its own data, in registration order. The reason to have the registry at all: one call says whether the whole workflow is sound, instead of one call per mode and a list kept by hand.\n\n\n\n\n\n","title":"ModularSystems.diagnose"},{"category":"method","location":"api/#ModularSystems.diagnose-Tuple{Problem}","page":"API","text":"diagnose(p::Problem) -> Diagnosis\n\nDiagnose a problem's block against its own data, which is the pairing that will actually be solved.\n\n\n\n\n\n","title":"ModularSystems.diagnose"},{"category":"method","location":"api/#ModularSystems.endogenize-Tuple{Block, Vararg{Any}}","page":"API","text":"endogenize(b::Block, items...) -> Block\n\nA copy of the block with those variables added to its unknown set. Accepts anything VariableGroup accepts — variables, containers, groups.\n\nNon-mutating: the original is untouched, so a calibration variant is a value rather than a copy that must be made before editing.\n\nThe result is not marked square, since adding an unknown changes the shape. Re-assert with assert_square! if it still holds.\n\n\n\n\n\n","title":"ModularSystems.endogenize"},{"category":"method","location":"api/#ModularSystems.exogenize-Tuple{Block, Vararg{Any}}","page":"API","text":"exogenize(b::Block, items...) -> Block\n\nA copy of the block with those variables removed from its unknown set. They are then substituted from the dataset at solve time like any other exogenous variable.\n\nRaises if a variable is determined by a constraint in this block: removing it would leave an equation determining something that is no longer solved for. Use swap to re-point that constraint at a different variable instead.\n\nThe result is not marked square; see endogenize.\n\n\n\n\n\n","title":"ModularSystems.exogenize"},{"category":"method","location":"api/#ModularSystems.fixed_point!-Tuple{Any, Any}","page":"API","text":"fixed_point!(step!, exchanged; kwargs...) -> ConvergenceReport\n\nRun step! repeatedly until the exchanged quantities stop moving, and raise if they do not.\n\nThis is how separate models are soft-linked: each pass solves them in turn, each consuming the other's last answer, until neither changes what it hands over. step! takes no arguments and does one pass — solving, and copying values between datasets.\n\nexchanged lists the cells that carry information between passes, as dataset => variable pairs; a variable container or a VariableGroup on the right stands for all its cells, and a single pair may be passed on its own. These are the feedback: the cells whose value at the start of a pass determines that pass. A quantity that a transfer overwrites before anything reads it carries no state and does not belong here — it is transferred, but it is not what the loop is converging.\n\nKeywords\n\natol = 1e-8, rtol = 1e-6: a pass has converged when every exchanged cell moved by no more than max(atol, rtol * |value|). atol must be positive, since it is what keeps the test meaningful for a cell converging to zero.\nmaxiter = 100: passes before giving up. Must be at least 1.\ndamping = 0.0: in [0, 1). Each exchanged cell is moved only (1 - damping) of the way to its new value, and the blend is written back so the next pass reads it.\nDamping fixes overshoot, not expansion. If a pass multiplies the error by m, damping makes that (1 - damping)·m + damping: for m < -1 — two models leapfrogging each other, the change alternating in sign — some damping brings it inside (-1, 1). For m > 1, where the link walks away in one direction, (1 - damping)·m + damping > 1 for every admissible value, and no amount of damping helps. A link that diverges monotonically is miscoupled, not under-damped.\nConvergence is always judged on the raw values the models produced, never on the blend. Judging the blend would scale the measured change by (1 - damping) and so scale the effective tolerance by 1 / (1 - damping), which is unbounded — a heavily damped run would report convergence on a link that never settles.\nraise = true: set false to return an unconverged report instead of raising. Reach for it when you intend to inspect history, not to carry on with the numbers.\n\nWhy it raises\n\nEvery individual solve in a diverging soft link converges and reports success — the models each answer the question they were given perfectly well, and the pair never agrees. There is no other symptom, so a loop that returns its last iterate after running out of passes hands back numbers that look exactly like a solution. See ConvergenceFailure.\n\nwarning: What is in the datasets afterwards\nOn success, the exchanged cells hold the raw values of the converging pass. On failure — or with raise = false — they hold the last iterate, which is not a solution.Note also that only the exchanged cells are checked. A link whose exchanged list names a cell nothing writes will sit at its initial value and be reported as converged on the first pass; the list has to name the feedback, and nothing can verify that for you.\n\nreport = fixed_point!([macro_data => pe]; damping = 0.5) do\n    solve!(macro_problem)\n    for s in S\n        energy_data[q[s]] = macro_data[E[s]]\n    end\n    solve!(energy_problem)\n    macro_data[pe] = energy_data[price]\nend\n\n\n\n\n\n","title":"ModularSystems.fixed_point!"},{"category":"method","location":"api/#ModularSystems.group_by-Tuple{IndexSet, Vararg{Symbol}}","page":"API","text":"group_by(s::IndexSet, names::Symbol...) -> Dict\n\nBucket coordinates by the named axes: the key is the projection onto those axes, the value is the IndexSet of full coordinates sharing it.\n\nThis is what replaces summing over one index of a sparse variable. Rather than ranging over an axis and guarding each access, take the coordinates that actually exist for the fixed part:\n\nby_product = group_by(pairs, :p)\nsum(use[k, t] for k in by_product[(:food,)])\n\n\n\n\n\n","title":"ModularSystems.group_by"},{"category":"method","location":"api/#ModularSystems.has_residuals-Tuple{Block}","page":"API","text":"has_residuals(b::Block) -> Bool\n\n\n\n\n\n","title":"ModularSystems.has_residuals"},{"category":"method","location":"api/#ModularSystems.has_tag-Tuple{JuMP.VariableRef, Tag}","page":"API","text":"has_tag(v::VariableRef, tag::Tag) -> Bool\n\n\n\n\n\n","title":"ModularSystems.has_tag"},{"category":"method","location":"api/#ModularSystems.highest_slot-Tuple{JuMP.AbstractModel}","page":"API","text":"highest_slot(model) -> Int\n\nThe highest slot issued by model, or 0 for a model with no variables. Walks every variable, so it is O(num_variables): call it when building a dataset, not on the access path.\n\n\n\n\n\n","title":"ModularSystems.highest_slot"},{"category":"method","location":"api/#ModularSystems.isclean-Tuple{Diagnosis}","page":"API","text":"isclean(x::Diagnosis) -> Bool\n\nWhether a diagnosis found no structural problem. A non-zero degrees of freedom is not a problem — that is the shape of an optimization block — but a contested equation or an undetermined unknown is, whatever the counts say.\n\n\n\n\n\n","title":"ModularSystems.isclean"},{"category":"method","location":"api/#ModularSystems.issquare-Tuple{Block}","page":"API","text":"issquare(b::Block) -> Bool\n\nWhether the block is a square system: no objective, and every solved constraint an equality paired with a distinct variable, together covering the unknowns.\n\n\n\n\n\n","title":"ModularSystems.issquare"},{"category":"method","location":"api/#ModularSystems.iswelldetermined-Tuple{Decomposition}","page":"API","text":"iswelldetermined(d::Decomposition) -> Bool\n\nWhether the decomposition found neither an over- nor an under-determined part, so the whole block can be solved subsystem by subsystem.\n\n\n\n\n\n","title":"ModularSystems.iswelldetermined"},{"category":"method","location":"api/#ModularSystems.largest_subsystem-Tuple{Decomposition}","page":"API","text":"largest_subsystem(d::Decomposition) -> Int\n\nThe size of the biggest simultaneous subsystem — the one number that says how much a block-triangular solve can save. A chain of scalar assignments decomposes to 1; a block that is irreducibly simultaneous decomposes to its own size and gains nothing.\n\n\n\n\n\n","title":"ModularSystems.largest_subsystem"},{"category":"method","location":"api/#ModularSystems.layout-Tuple{JuMP.AbstractModel}","page":"API","text":"layout(model) -> ModelLayout\n\nThe model's layout, created and cached in model.ext on first use.\n\n\n\n\n\n","title":"ModularSystems.layout"},{"category":"method","location":"api/#ModularSystems.modes-Tuple{ModelSpec}","page":"API","text":"modes(spec::ModelSpec) -> Vector{Symbol}\n\nThe registered mode names, in registration order.\n\n\n\n\n\n","title":"ModularSystems.modes"},{"category":"method","location":"api/#ModularSystems.note_slot!-Tuple{ModularSystems.ModelLayout, Integer}","page":"API","text":"note_slot!(l::ModelLayout, s::Integer) -> Int\n\nRecord that slot s is in use and return the layout's high-water mark. Lets a dataset grow to fit a variable declared after it was built, without rescanning the model.\n\n\n\n\n\n","title":"ModularSystems.note_slot!"},{"category":"method","location":"api/#ModularSystems.overdetermined-Tuple{Decomposition}","page":"API","text":"overdetermined(d::Decomposition) -> SubSystem\n\nThe equations left with no variable to determine, and the variables they compete for. Non-empty means the block asks more of those variables than they can satisfy — the usual cause is an equation written twice, or a variable that should have been an unknown and was not.\n\n\n\n\n\n","title":"ModularSystems.overdetermined"},{"category":"method","location":"api/#ModularSystems.pairings-Tuple{Block}","page":"API","text":"pairings(b::Block) -> VariableGroup\n\nThe variables paired with a solved constraint, in constraint order.\n\n\n\n\n\n","title":"ModularSystems.pairings"},{"category":"method","location":"api/#ModularSystems.ready-Tuple{ModelSpec}","page":"API","text":"ready(spec::ModelSpec) -> Vector{Symbol}\n\nThe modes that could be solved right now, in registration order: those whose block is sound and whose data has every exogenous value the block needs.\n\nThis is what a workflow's ordering looks like when it is derived rather than declared. A calibration is ready before the baseline that consumes its output, because the baseline's parameters are still unset — nobody has to say so, and nobody can say it wrongly. Re-ask after each solve and the order falls out.\n\nready(spec)                  # [:calibration]\nsolve!(spec, :calibration)\nready(spec)                  # [:calibration, :baseline]\n\nUse diagnose on the spec for why a mode is not ready.\n\n\n\n\n\n","title":"ModularSystems.ready"},{"category":"method","location":"api/#ModularSystems.register!-Tuple{ModelSpec, Symbol, Problem}","page":"API","text":"register!(spec::ModelSpec, name::Symbol, p::Problem; about = \"\") -> ModelSpec\nregister!(spec::ModelSpec, name::Symbol, block, data; about = \"\", kwargs...) -> ModelSpec\n\nAdd a named mode. The second form builds the Problem in place, taking its keywords.\n\nabout is one line of prose, shown when the spec is displayed. Worth writing: composition through + is not recoverable from the result, so two modes that answer completely different questions — a steady-state calibration and a dynamic one, say — otherwise print as two identical rows of counts. The name is the identity; this is the explanation.\n\nRaises on a name already registered rather than replacing it: overwriting a mode silently is how a workflow ends up running something other than what its script says. Remove it first with unregister! if replacing is what you mean.\n\n\n\n\n\n","title":"ModularSystems.register!"},{"category":"method","location":"api/#ModularSystems.residual-Tuple{JuMP.VariableRef}","page":"API","text":"residual(v::VariableRef) -> Union{VariableRef,Nothing}\n\nThe residual belonging to a variable, or nothing if it has none. Created by with_residuals.\n\n\n\n\n\n","title":"ModularSystems.residual"},{"category":"method","location":"api/#ModularSystems.residuals-Tuple{Block}","page":"API","text":"residuals(b::Block) -> VariableGroup\n\nThe residuals carried by this block's constraints, in constraint order. Empty for a block that with_residuals has not been applied to.\n\n\n\n\n\n","title":"ModularSystems.residuals"},{"category":"method","location":"api/#ModularSystems.select_axes-Tuple{IndexSet, Vararg{Symbol}}","page":"API","text":"select_axes(s::IndexSet, names::Symbol...) -> IndexSet\n\nProject onto the named axes, dropping duplicates that the projection creates. select_axes(pairs, :p) turns a product-industry pattern into the set of products that occur in it.\n\n\n\n\n\n","title":"ModularSystems.select_axes"},{"category":"method","location":"api/#ModularSystems.set_bounds!-Tuple{Dataset, JuMP.AbstractVariableRef}","page":"API","text":"set_bounds!(d::Dataset, v; lower = nothing, upper = nothing)\n\nSet problem bounds on a variable. A keyword left nothing leaves that side untouched; use clear_bounds! to remove one. Raises if the resulting interval is empty, since an empty interval otherwise surfaces much later as an unexplained infeasibility.\n\n\n\n\n\n","title":"ModularSystems.set_bounds!"},{"category":"method","location":"api/#ModularSystems.set_bounds!-Tuple{Dataset, VariableGroup}","page":"API","text":"set_bounds!(d::Dataset, g::VariableGroup; lower = nothing, upper = nothing)\n\nSet the same problem bounds on every variable in a group. Cell-by-cell bound setting is unusable at model scale, so this is the form that gets used.\n\n\n\n\n\n","title":"ModularSystems.set_bounds!"},{"category":"method","location":"api/#ModularSystems.set_objective!-Tuple{Block, MathOptInterface.OptimizationSense, Any}","page":"API","text":"set_objective!(b::Block, sense, func) -> Block\n\nAttach an objective, making the block an optimization problem rather than a square system. sense is MOI.MIN_SENSE or MOI.MAX_SENSE.\n\n\n\n\n\n","title":"ModularSystems.set_objective!"},{"category":"method","location":"api/#ModularSystems.set_optimizer_factory!-Tuple{JuMP.AbstractModel, Any}","page":"API","text":"set_optimizer_factory!(model, optimizer) -> model\n\nRemember the optimizer a solve should use for this model, so solve need not be told each time. A solve builds its own intermediate model, so it cannot inherit the optimizer attached to the model the variables live in.\n\n\n\n\n\n","title":"ModularSystems.set_optimizer_factory!"},{"category":"method","location":"api/#ModularSystems.set_unknowns!-Tuple{Block, VariableGroup}","page":"API","text":"set_unknowns!(b::Block, g) -> Block\n\nDeclare the variables the block solves for. Required once any solved constraint is unpaired; otherwise the unknowns are derived from the pairings.\n\n\n\n\n\n","title":"ModularSystems.set_unknowns!"},{"category":"method","location":"api/#ModularSystems.slot-Tuple{JuMP.AbstractVariableRef}","page":"API","text":"slot(v) -> Int\n\nThe storage slot of a variable. JuMP issues MOI.VariableIndex values sequentially from 1, so the index doubles as a dense array position and no separate id map is needed.\n\nwarning: Warning\nDeleting a variable leaves its slot permanently unused — MOI never reuses an index — so after a deletion the highest slot exceeds num_variables(model). Size storage by the highest slot, never by num_variables.\n\n\n\n\n\n","title":"ModularSystems.slot"},{"category":"method","location":"api/#ModularSystems.solve!-Tuple{Block, Dataset}","page":"API","text":"solve!(b::Block, d::Dataset; kwargs...) -> Dataset\n\nSolve a block against a dataset, writing the solution into d. See solve for the form that leaves d untouched.\n\nExogenous variables — anything not an unknown of the block — are substituted from the dataset, so the intermediate model holds only the unknowns. Bounds applied to an unknown are the intersection of its intrinsic JuMP bounds with the dataset's problem bounds.\n\nKeywords\n\noptions::SolveOptions: how the solve is run. Every field of SolveOptions may also be passed here directly as a keyword, which overrides options for this call — so solve!(b, d; silent = false) and solve!(b, d; options = SolveOptions(o; silent = false)) mean the same thing, and the settings can be named once and reused.\nstart_values::Dataset: starting points, falling back to d's own values. Data rather than a setting, which is why it is a keyword of its own and not a field of SolveOptions.\nstrategy::SolveStrategy = Monolithic(): which algorithm. Also a keyword of its own, because it has preconditions and so is a property of this problem rather than a reusable setting — see SolveOptions.\n\nnote: Note\noptimizer names the solver for this solve only and does not attach it to the model. set_optimizer_factory! is what sets a model's default.\n\nA block carrying an objective is solved as an optimization problem: the objective is substituted from the dataset like any other expression and attached to the same intermediate model, so the two paths differ only at the tail. meta.objective_value records its value.\n\n\n\n\n\n","title":"ModularSystems.solve!"},{"category":"method","location":"api/#ModularSystems.solve-Tuple{Block, Dataset}","page":"API","text":"solve(b::Block, d::Dataset; kwargs...) -> Dataset\n\nSolve a block against a dataset, returning a solved copy and leaving d untouched. Takes the same keywords as solve!, whose docstring describes them.\n\n\n\n\n\n","title":"ModularSystems.solve"},{"category":"method","location":"api/#ModularSystems.solve-Tuple{ModelSpec, Symbol}","page":"API","text":"solve(spec::ModelSpec, name::Symbol; kwargs...) -> Dataset\nsolve!(spec::ModelSpec, name::Symbol; kwargs...) -> Dataset\n\nSolve one registered mode. solve returns a solved copy; solve! writes into that mode's dataset, which is what lets the next mode start from it.\n\n\n\n\n\n","title":"ModularSystems.solve"},{"category":"method","location":"api/#ModularSystems.solve-Tuple{Problem}","page":"API","text":"solve(p::Problem; kwargs...) -> Dataset\nsolve!(p::Problem; kwargs...) -> Dataset\n\nSolve a problem, using its own dataset, starting values and options. solve returns a solved copy and leaves the problem's dataset untouched; solve! writes into it.\n\nAny keyword accepted by solve! overrides the problem's own options for this call.\n\n\n\n\n\n","title":"ModularSystems.solve"},{"category":"method","location":"api/#ModularSystems.solved_constraints-Tuple{Block}","page":"API","text":"solved_constraints(b::Block)\nchecked_constraints(b::Block)\n\nThe constraints of a block by role.\n\n\n\n\n\n","title":"ModularSystems.solved_constraints"},{"category":"method","location":"api/#ModularSystems.subsystems-Tuple{Decomposition}","page":"API","text":"subsystems(d::Decomposition) -> Vector{SubSystem}\n\nThe well-determined part of a decomposition, in solve order: each subsystem's inputs are determined by the ones before it. Every entry is square.\n\n\n\n\n\n","title":"ModularSystems.subsystems"},{"category":"method","location":"api/#ModularSystems.swap-Tuple{Block, Vararg{Pair}}","page":"API","text":"swap(b::Block, pairs::Pair...) -> Block\n\nA copy of the block with each new => old pair exchanged: new becomes an unknown, old becomes exogenous, and the constraint that determined old now determines new.\n\nThis is calibration. The equations do not change — only which variable each one is understood to determine, and which variables are solved for:\n\ncalibration = swap(behavioural, mu => L, rho => w)\n\nEither side may be a variable, a container or a group. Group sides are paired in iteration order and must have equal length, which is why a VariableGroup is ordered. Use @group when the cells need selecting:\n\ncalibration = swap(behavioural, @group(mu[j in J]) => @group(L[j in J]))\n\nUnlike endogenize and exogenize, a swap preserves the shape of the system — one pairing is re-pointed, the counts do not move — so a block declared square stays declared square and is re-checked here.\n\n\n\n\n\n","title":"ModularSystems.swap"},{"category":"method","location":"api/#ModularSystems.tag!-Tuple{JuMP.AbstractModel, Tag, Vararg{Any}}","page":"API","text":"tag!(model, tag::Tag, items...) -> Tag\n\nAttach a tag to every variable cell in items, which are flattened exactly as for VariableGroup — variables, containers, groups, or any iterable of those.\n\nAttaching after declaration rather than at it is deliberate: it needs no macro, so JuMP.@variables is never shadowed and every JuMP declaration form keeps working.\n\n\n\n\n\n","title":"ModularSystems.tag!"},{"category":"method","location":"api/#ModularSystems.tagged-Tuple{JuMP.AbstractModel, Tag}","page":"API","text":"tagged(model, tag::Tag) -> VariableGroup\n\nEvery variable carrying a tag, in the model's own variable order so the result is deterministic.\n\nWalks the model's variables rather than keeping a second index by tag: tagging happens while a model is being written, never in a solve, and one source of truth cannot fall out of step with itself.\n\n\n\n\n\n","title":"ModularSystems.tagged"},{"category":"method","location":"api/#ModularSystems.tags-Tuple{JuMP.VariableRef}","page":"API","text":"tags(v::VariableRef) -> Set{Tag}\n\nEvery tag on a variable cell, empty if it has none.\n\n\n\n\n\n","title":"ModularSystems.tags"},{"category":"method","location":"api/#ModularSystems.underdetermined-Tuple{Decomposition}","page":"API","text":"underdetermined(d::Decomposition) -> SubSystem\n\nThe variables no equation is left to determine, and the equations that could have determined them. Non-empty means the block does not pin those variables down.\n\n\n\n\n\n","title":"ModularSystems.underdetermined"},{"category":"method","location":"api/#ModularSystems.unknowns-Tuple{Block}","page":"API","text":"unknowns(b::Block) -> VariableGroup\n\nThe variables the block solves for: the declared set if there is one, otherwise the pairings.\n\nRaises when some solved constraint is unpaired and no set has been declared — the block cannot then say what it is solving for, and guessing would silently change the problem.\n\n\n\n\n\n","title":"ModularSystems.unknowns"},{"category":"method","location":"api/#ModularSystems.unregister!-Tuple{ModelSpec, Symbol}","page":"API","text":"unregister!(spec::ModelSpec, name::Symbol) -> ModelSpec\n\nRemove a mode. Raises if it is not there, so a typo does not pass quietly.\n\n\n\n\n\n","title":"ModularSystems.unregister!"},{"category":"method","location":"api/#ModularSystems.untag!-Tuple{JuMP.AbstractModel, Tag, Vararg{Any}}","page":"API","text":"untag!(model, tag::Tag, items...) -> Tag\n\nRemove a tag from every variable cell in items. Variables that do not carry it are left alone.\n\n\n\n\n\n","title":"ModularSystems.untag!"},{"category":"method","location":"api/#ModularSystems.validate-Tuple{Block}","page":"API","text":"validate(b::Block) -> Block\n\nCheck a block is well formed, and that it is square if it was built with square = true. Called by solve; call it directly when building a block programmatically and you want the error where the block is defined rather than where it is solved.\n\n\n\n\n\n","title":"ModularSystems.validate"},{"category":"method","location":"api/#ModularSystems.variables_in-Tuple{Any}","page":"API","text":"variables_in(x) -> Set{VariableRef}\n\nEvery variable appearing in a JuMP expression, a Constraint or a Block. For a block this includes checks and the objective, not only the solved constraints.\n\n\n\n\n\n","title":"ModularSystems.variables_in"},{"category":"method","location":"api/#ModularSystems.with_residuals-Tuple{Block, Vararg{Dataset}}","page":"API","text":"with_residuals(b::Block, datasets...; suffix = \"_J\") -> Block\n\nA copy of the block with a residual variable added to every paired solved constraint. The residual is added to the constraint function, so an equation written x == rhs becomes x + residual(x) == rhs: the residual is the amount by which the left side falls short.\n\nResiduals are exogenous and normally zero, so they change nothing until you ask them to. Any datasets passed are given zeros for the new residuals; without that, the first solve raises on an exogenous variable with no value.\n\nOpt-in, and paired-only. A residual doubles the variable count and is meaningless for a constraint that determines nothing, so it is never created automatically.\n\nWhat they are for is locating inconsistent data. Exogenise a variable at its observed value, make its residual the unknown instead, and solve: the residual reads off how far the data misses the equation. That is a swap:\n\nb = with_residuals(behavioural, observed)\ncheck = swap(b, residual(L[1]) => L[1])\nsolve!(check, observed)      # observed[residual(L[1])] is now the inconsistency\n\nCalling it twice is harmless: a constraint that already carries its residual is left alone.\n\n\n\n\n\n","title":"ModularSystems.with_residuals"},{"category":"macro","location":"api/#ModularSystems.@block-Tuple{Any, Any}","page":"API","text":"@block model begin ... end\n@block dataset begin ... end\n\nBuild a Block. Each line is one entry:\n\nForm Meaning\nx, expr constraint paired with x\nx[i in I], expr indexed, paired with x[i]\n[i in I], expr indexed, unpaired\nexpr scalar, unpaired\n@check expr \"msg\" evaluated after a solve, never solved\n@unknowns ... declares the unknown set\n@objective Min expr attaches an objective\n@square asserts squareness, checked as the block is built\n\nA comma-separated head names the variable a constraint determines; brackets alone give index sets and leave the constraint unpaired. @unknowns is required once any solved constraint is unpaired — which is what turns a dropped variable name into a degrees-of-freedom mismatch rather than a silent change of problem. A filter goes after ;, as in x[t in T; t > t0].\n\nproduction = @block data begin\n    @square\n    L[j in J], L[j] == mu[j] * (w[j] / p)^-sigma * Y\n    Y,        p * Y == sum(w[j] * L[j] for j in J)\n    p,        p == 1\n    @check C == sum(Cj[j] for j in J)  \"consumption aggregation\"\nend\n\n\n\n\n\n","title":"ModularSystems.@block"},{"category":"macro","location":"api/#ModularSystems.@declare-Tuple{Any, Any}","page":"API","text":"@declare model begin ... end\n@declare model::tag begin ... end\n@declare dataset::(tag1, tag2) begin ... end\n\nDeclare many variables at once, each with an optional description, and tag every variable the block declares.\n\nEach row is a JuMP.@variable declaration — bounds, start, containers and all — optionally followed by a description:\n\n@declare model begin\n    L[j in J],          \"Labour demand\"\n    w[j in J] >= 0,     \"Wage\"\n    Y,                  \"Output\"\n\n    N[j in J],          \"Labour force (exogenous)\"\n    sigma,              \"Substitution elasticity\"\nend\n\nThe rows are handed to JuMP.@variables unchanged, so every JuMP declaration form keeps working and the return value is the same: a tuple of the declared containers, in row order. A description is attached with describe! and a tag with tag!, which is also how either is added to a variable declared some other way — this macro is a shorthand, never a second mechanism.\n\nTags after :: apply to every variable the block declares, including anonymous ones:\n\n@declare model::Quantity begin\n    L[j in J], \"Labour demand\"\n    Y,         \"Output\"\nend\n\nNothing here shadows JuMP.@variables, which remains available and unmodified.\n\nExamples\n\njulia> using JuMP\n\njulia> model = Model();\n\njulia> const Quantity = Tag(:quantity);\n\njulia> J = 1:2;\n\njulia> L, w, Y = @declare model::Quantity begin\n           L[j in J],      \"Labour demand\"\n           w[j in J] >= 0, \"Wage\"\n           Y,              \"Output\"\n       end;\n\njulia> description(L[1])\n\"Labour demand\"\n\njulia> has_tag(w[2], Quantity)\ntrue\n\njulia> lower_bound(w[1])\n0.0\n\n\n\n\n\n","title":"ModularSystems.@declare"},{"category":"macro","location":"api/#ModularSystems.@group-Tuple","page":"API","text":"@group items...\n\nBuild a VariableGroup using the same index syntax as @block, so mu[j in J] and K[t in T; t > t0] mean here what they mean there.\n\nquantities   = @group L, Y, C\nelasticities = @group sigma, mu[j in J]\n\n\n\n\n\n","title":"ModularSystems.@group"},{"category":"section","location":"design/#Design","page":"Design","text":"The record of what has been decided and what has not. Decisions are appended here with their reasoning, so a later reader can see what a change would cost. Open questions live in notes/TODO.md; this page carries only what is settled.","title":"Design"},{"category":"section","location":"design/#Scope","page":"Design","text":"One package, no sibling packages. Optional dependencies are reached through package extensions in ext/, which live in this repository and load only when the optional package is present.\n\nThe priorities, in the order that breaks ties: speed and efficiency; modularity and flexibility; and being easy to read, write and adjust while a model is being developed. Decisions below are argued against these, and a measurement beats an argument from elegance.","title":"Scope"},{"category":"section","location":"design/#Settled","page":"Design","text":"","title":"Settled"},{"category":"section","location":"design/#Clean-room-implementation","page":"Design","text":"The package borrows concepts from SquareModels.jl — equation blocks, endo-exo swapping, a model-level data dictionary — and no code. This costs re-derivation and buys freedom in the interface, which is the reason the package exists at all.","title":"Clean-room implementation"},{"category":"section","location":"design/#The-package-sits-on-JuMP","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C1.\n\nThe three candidates were JuMP, MathOptInterface alone, and a solver-agnostic core with JuMP as a package extension.\n\nJuMP wins on the thing that matters for a small package: it already supplies the expression macros, the variable and constraint containers, and the solver interfaces, and its users arrive fluent in them. MathOptInterface alone would mean rebuilding that surface to no clear benefit, and a solver-agnostic core would mean maintaining two interfaces before we know what the first one should look like. SquareModels.jl made the same choice, which is also a compatibility argument: a model written against one is recognisable to a reader of the other.\n\nWhat this commits us to: the JuMP model is the state, and JuMP's @variable/@constraint idiom shapes ours. The cost, if it turns out to be wrong, is an interface rewrite rather than a rewrite of the solving logic — acceptable at this stage. MathOptInterface is not a direct dependency yet; add it when a solver attribute actually needs reaching for, not pre-emptively.","title":"The package sits on JuMP"},{"category":"section","location":"design/#Solvers-and-plotting-go-in-ext/","page":"Design","text":"Weak dependencies with package extensions, the pattern SquareModels.jl uses for GAMS, GDXInterface and Makie. A user who only wants to build and solve a model should not pay for a plotting stack. Nothing here yet — this is the shape to follow when the first integration arrives.","title":"Solvers and plotting go in ext/"},{"category":"section","location":"design/#One-Block-type,-with-the-pairing-optional","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C2.\n\nA block is a collection of constraints over a JuMP model, not a square map. Each constraint carries two independent pieces of metadata: which variable it is paired with, if any, and whether it is solved (enters the system) or checked (evaluated after a solve). A block also carries an explicitly declared set of unknowns and an optional objective.\n\nSquareness is therefore a predicate, not an invariant: issquare asks whether there is no objective, every solved constraint is an equality with a distinct paired variable, and those pairings cover the unknowns. The number that generalises is the degrees of freedom, which is defined whether or not the system is square.\n\nThe alternative was two types — a square block and an optimization block — with a shared interface. Rejected because composing a square behavioural block with an estimation objective is the case that motivates the flexibility, and two types make that a conversion rather than a sum.\n\nWhat this buys, beyond admitting NLP problems: inequality constraints become ordinary solved constraints instead of being confined to test constraints, and a \"test constraint\" stops being a special form — it is a constraint whose role is check.\n\nWhat it costs: SquareModels raises a non-square error at block construction, naming that block, before assembly. A predicate defers that to solve time, about the whole system. The mitigation is to let a block declare its own intent — a block written to be square says so and is checked immediately — so the local error is available without being compulsory. This has to be cheap and visible in the @block grammar, not a keyword nobody remembers.","title":"One Block type, with the pairing optional"},{"category":"section","location":"design/#The-pairing-is-documentation-plus-a-precondition","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C3.\n\nIt records which equation is understood to determine which variable, it names constraints in solver output and diagnostics, and it is what the square solver checks before running. It does not order the system, drive the write-back, or seed starting values. In a block with an objective it is inert.\n\nThis was checked against SquareModels.jl rather than assumed, because the opposite choice — a pairing that is semantic everywhere — would have been the more ambitious package. Its solve path keys everything on the unknown set: transform_expr branches on whether a variable is an unknown (map it to the intermediate model) or not (substitute its data value), and the write-back iterates the unknown map in dictionary order. The pairing appears only in the constructor check, in set_name(con, name(endogenous[i])), in the positional alignment of residual variables, and in the diagnostics. So the substitution-and-write-back spine is shared between a square solve and an optimization solve, and the two differ only at the tail.\n\nA consequence for endo-exo swapping: the primitive is moving a variable between the unknown set and the fixed set — fix and free over a variable group — which means the same thing in both kinds of system. Re-pointing a pairing is the square-case extra, not the operation itself.","title":"The pairing is documentation plus a precondition"},{"category":"section","location":"design/#Variable-groups-are-a-core-type","page":"Design","text":"Decided 2026-09-14.\n\nComposable collections of variables, assembled after declaration and passed to fix, free, solve and bound-setting. Once the unknowns are declared rather than derived from pairings, naming a set of variables is something the interface does constantly. The type is specified below under Variable groups, and index sets as their coordinate twin; note that the \"named\" in the original form of this decision did not survive — groups carry no name field.","title":"Variable groups are a core type"},{"category":"section","location":"design/#Problem-bounds-live-in-the-dataset","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C9.\n\nTwo different things were being called \"bounds\":\n\nIntrinsic bounds — K ≥ 0, share ∈ [0, 1] — are properties of the variable, true in every problem it appears in. They stay on the JuMP variable declaration, where JuMP validates them and where they are visible next to the variable.\n\nProblem bounds — \"in this calibration keep σ ∈ [0.1, 10]\", \"K ≥ 0.5 · K_baseline\" — are properties of the problem instance, are often derived from data, and differ across scenarios. They live in the dataset.\n\nThe objection to putting them in the dataset is that a calibration block and a behavioural block are solved against the same data with different unknowns, so bounds look like they should be per-block. They should not, because a bound only ever bites on a variable that is unknown in that solve, and the unknown set is a block property. A variable that a block treats as exogenous is substituted as a number and never enters the solve model, so its dataset bound is irrelevant rather than conflicting. Dataset bounds therefore specialise per block on their own, with no merge rule and no second home. The rare case of two blocks wanting different ranges for the same unknown is handled by copying the dataset.\n\nPutting them on the block was rejected for a second reason: data-derived bounds would make a block carry numbers, and the point of a block is that it is the equations.\n\nStorage is layers over one key space — a dense value vector, sparse lower and upper maps, and per-dataset solve metadata, all keyed on the variable id the layout already assigns:\n\nstruct Dataset{T}\n    layout::ModelLayout                # shared across datasets\n    values::Vector{Union{T,Nothing}}   # dense: a value for nearly every variable\n    lower::Dict{Int,Float64}           # sparse: bounds are genuinely rare\n    upper::Dict{Int,Float64}\n    meta::SolveMetadata                # termination status, objective value, solve time\nend\n\nBounds are sparse because most variables have none, and sharing the key space means there is no second lookup mechanism to keep in sync with the model layout. The rejected alternative was one (value, lower, upper) record per cell: it triples memory for a field absent on most cells, and it makes dataset arithmetic ambiguous.","title":"Problem bounds live in the dataset"},{"category":"section","location":"design/#Arithmetic-returns-a-value-only-dataset","page":"Design","text":"scenario ./ baseline .- 1 must give multipliers; bounds do not divide, and a ratio of two scenarios is not a scenario, so bounds and solve metadata are dropped from an arithmetic result. copy preserves every layer. Any data I/O that is added later must state which layers round-trip.\n\nStarting values stay a separate dataset rather than becoming a layer. start_values = baseline — start the scenario from the baseline solution — is the common idiom and it genuinely is another dataset.\n\nfix/free stays strictly about membership of the unknown set. A degenerate bound [v, v] is a second way to express the same intent, slower and harder to diagnose, so it warns rather than acting as a fix.","title":"Arithmetic returns a value-only dataset"},{"category":"section","location":"design/#A-binding-bound-on-a-square-solve-is-an-error","page":"Design","text":"Decided 2026-09-14.\n\nThe effective bound on an unknown is the intersection of its intrinsic and dataset bounds; an empty intersection is a named error rather than a mysterious infeasibility. If a bound is active at the solution of a square system, the answer is not the solution of that system — the problem has silently become a complementarity problem, and the solver reports success. That is a wrong answer delivered quietly, which is worse than an interruption, so it raises. On the optimization path a binding bound is normal and is reported in the dataset's solve metadata, not raised. An opt-out keyword follows presolve_diagnostics.\n\nOne thing this cost in practice, recorded because it would silently disable the check: the tolerance must be looser than the solver's own bound relaxation. An interior-point solver does not land on a bound — Ipopt relaxes bounds and stops slightly outside, measured at about 1.7e-8 below a lower bound. A tolerance at solver precision therefore detects nothing at all. The default is 1e-6, scaled by the magnitude of the bound, and test/optimize.jl pins the behaviour so tightening it cannot pass unnoticed.\n\nThis diagnostic is the strongest argument for the split above: it has to distinguish a bound imposed for this problem from a bound intrinsic to the variable, which is only possible if the two are stored separately. SquareModels.jl cannot perform this check — solve.jl has no dual or binding-bound logic, and bounds exist only as model state.","title":"A binding bound on a square solve is an error"},{"category":"section","location":"design/#Continuous-variables-only","page":"Design","text":"Decided 2026-09-14.\n\nNo integer or binary variables; mixed-integer problems are out of scope. diagnose may treat integrality as an illegal shape and say so. The layer design would extend to per-variable integrality exactly as it extends to bounds, so this can be revisited without rework if it ever needs to be.","title":"Continuous variables only"},{"category":"section","location":"design/#The-@block-grammar","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C5.\n\nOne macro covers both kinds of system. The grammar itself is documented in the manual under Reference: the @block grammar; what follows is why it has that shape.\n\nproduction = @block data begin\n    @square\n\n    L[j ∈ J],  L[j] == μ[j] * (w[j] / p)^-σ * Y\n    w[j ∈ J],  L[j] == ρ[j] * N[j]\n    Y,         p * Y == ∑(w[j] * L[j] for j ∈ J)\n    p,         p == 1\n\n    @check C == ∑(Cj[j] for j ∈ J)  \"consumption aggregation\"\nend\n\nestimation = @block data begin\n    @unknowns σ, μ[j ∈ J]\n    @objective Min ∑((w[j] - ŵ[j])^2 for j ∈ J)\n\n    [j ∈ J],  L[j] == μ[j] * (w[j] / p)^-σ * Y\n    p == 1\nend\n\nWhy this shape:\n\nPairing is optional by syntax, not by a flag. A comma-separated head means the constraint determines that variable; no head means it does not. A square block therefore reads exactly as it does in SquareModels.jl — the common case did not get worse to buy the optimization case.\n\nThe unpaired indexed form is JuMP's own. @constraint(model, [i = 1:3], x[i] >= 0) is how JuMP writes an anonymous indexed constraint, so the form we had to invent is one a JuMP user already knows, and paired versus unpaired is exactly whether a name precedes the brackets.\n\n@check takes its constraint as an argument. SquareModels' @test_constraint is a bare statement that modifies the following line, which is why its documentation has to say the macro call must be a separate statement directly before the entry. Taking the constraint as an argument makes the association syntactic, so a blank line, a reorder or a stray comment cannot break it. The shape @check expr \"message\" deliberately mirrors @assert. Tolerances ride as keywords: @check(expr, \"msg\"; atol = 1e-8).\n\n@-prefixed lines are declarations; bare lines are constraints. One visual rule covers @unknowns, @objective and @square. @check is the deliberate exception and announces itself by taking a constraint rather than standing alone.\n\n@square is the mitigation this page promised. Moving squareness from invariant to predicate costs the construction-time error; one line at the top of a block restores it for blocks that want it, without forcing it on blocks that do not.\n\nNone of these need to exist as macros — @block walks its body and matches on Expr(:macrocall, Symbol(\"@check\"), …). Define them anyway, erroring with \"only valid inside @block\", so a misplaced one fails with a sentence rather than an UndefVarError.\n\nThe known cost: a[t ∈ T], … and [t ∈ T], … differ by one token, so a dropped variable name silently converts a paired constraint into an unpaired one. Under @square that is an immediate error. Otherwise the safety net is that @unknowns is mandatory once anything is unpaired, so the mistake surfaces as a degrees-of-freedom mismatch against a declared set instead of passing quietly. That is the reason @unknowns is required rather than inferred.\n\nA documentation obligation, not a grammar one: σ >= 0.1 is a legal inequality constraint but it is not a bound. Bounds are dataset state set with set_bounds!, and only bounds get the binding-bound diagnostic. Same mathematics, different machinery, and users will write the constraint and wonder why the diagnostic stayed quiet.","title":"The @block grammar"},{"category":"section","location":"design/#Variable-groups,-and-index-sets-as-their-coordinate-twin","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C8.\n\nstruct VariableGroup\n    model::JuMP.AbstractModel\n    vars::Vector{VariableRef}   # ordered, unique\n    set::Set{VariableRef}       # O(1) membership\nend\n\nElements are variable cells (μ[1], K[2030]), not containers, because every call site — @unknowns, set_bounds!, fix/free — selects cells. Built with @group, which shares the index-spec parser with @block so μ[j ∈ J] means the same thing everywhere, or with VariableGroup(itr) for programmatic construction; the split mirrors JuMP's own @variable / add_variable.\n\nOrdered, with a membership set beside it — the same vector-plus-set pattern Block uses. Order is not needed to solve, since the solve map is a Dict, but it is needed for deterministic diagnostics and for the swap, which pairs two selections in iteration order. A group is a value frozen at construction, not a query re-evaluated against the model: membership that could change between declaring unknowns and solving is a wrong answer rather than an error. It validates that all its variables share one model. It implements iteration, length, in and getindex but does not subtype AbstractVector, which would invite push!, similar and array broadcasting semantics that would then have to be supported or explained away.\n\nNo name field. The Julia binding is the name; a name field can disagree with it. The cost is that diagnose cannot say \"3 unknowns in elasticities never appear in a constraint\" — it lists variables instead. Accepted deliberately.\n\nThere are no constraint groups: Block already is the collection of equations, with + as its composition. Groups complete that symmetry rather than duplicating it.\n\nTags are kept, but attached by function rather than by shadowing JuMP.@variables.\n\ntag!(model, GrowthAdjusted, qGDP, vGDP)\ntag!(model, FlatForecast, K[t ∈ T; t > t₀])\ntagged(model, GrowthAdjusted)                # -> VariableGroup\n\nStorage goes in model.ext, which is JuMP's sanctioned extension point, so the tag machinery itself costs about 120 lines and requires no macro. What requires shadowing is the declaration-site :: syntax, not tags — these are separable, and SquareModels.jl conflates them because sparse container construction, which genuinely does need to own the declaration site, lives in the same macro.\n\nTwo further reasons for the function form. SquareModels keys metadata on the base variable symbol (Dict{Symbol, VariableMetadata}), so its tags are per container: X[2020] returns the tags of X, and a single cell cannot be tagged. Keying on VariableRef puts tags at the same granularity as groups, which is what lets tagged return a genuine group. And a macro named @variables would collide with JuMP's export, forcing every user to qualify — SquareModels' own README never writes using JuMP, only import JuMP plus a selective using JuMP: Model, set_silent, which is that collision being worked around. That particular cost is avoidable by choosing a different name and should not be counted against shadowing in general.\n\nWhat is given up: attaching a tag at the declaration site. It goes on a following line instead. Descriptions are the same story, and get the same treatment — describe!(model, qGDP, \"Real GDP\"), same store, no macro.\n\nIndexSet is the coordinate-level twin of VariableGroup — an ordered set of coordinate tuples with named axes, carrying the same ∪ / ∩ / setdiff, plus projection and grouping by axis, which only make sense at the coordinate level. A VariableGroup is what you get by applying an IndexSet to a container. Its role in sparsity is the next decision.","title":"Variable groups, and index sets as their coordinate twin"},{"category":"section","location":"design/#Sparsity-is-handled-by-coordinate-axes","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C10.\n\nDeclaring a sparse variable with a filter makes JuMP evaluate the predicate over the full product of the axes. Measured, at 50 periods with a tridiagonal pattern:\n\nI × J × T combinations variables filtered @variable coordinate axis\n50×50×50 125,000 7,400 46.7 ms 8.3 ms\n100×100×50 500,000 14,900 109.5 ms 14.4 ms\n400×400×50 8,000,000 59,900 1306.6 ms 94.8 ms\n\nThe script is kept at archive/sparseDeclarationBenchmark.jl, so the table can be rerun rather than believed. Filtered declaration scales with the product of the axes; declaring over a coordinate axis (@variable(model, use[pairs, t = years])) scales with the number of variables. That is plain JuMP — no macro, no custom container.\n\nIt also removes the second half of the problem. A coordinate-keyed container is dense over its own axes, so there are no absent cells to raise KeyError and nothing for a Zero() sentinel to paper over. Equations iterate patterns instead of guarding products:\n\n∑(use[k, t] * price[k, t] for k in patternA ∩ patternB)\n\nwhich is also clearer than the sentinel: the coordinates being summed are visible. Zero() makes a sum over the dense product silently correct, which is convenient until the pattern is not what was assumed and the sum quietly omits terms.\n\nWhat is given up is notation: x[p, i, t] on a sparse variable becomes x[k, t] with k a coordinate. That is the entire cost, and it is the only thing SquareModels' ~710 lines of sparse machinery buys that this does not.\n\nTwo implementation constraints follow, cheap now and expensive later. @block must ask a container for its stored keys through a generic interface, never by dispatching on a concrete sparse type — SquareModels' core names SparseZeroArray in _variable_refs, _key_of, _all_keys and _ndims, which is what makes its sparse layer non-optional in practice. And the expression walkers should tolerate an additive-identity sentinel they do not define, so a notation layer could be added later by a package extension rather than a rewrite.","title":"Sparsity is handled by coordinate axes"},{"category":"section","location":"design/#Composition","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C6.\n\na + b concatenates constraints over one model. A variable determined in both blocks raises, so an equation cannot be silently dropped.\n\nAt most one objective across a sum. Two objectives raise rather than being summed: adding objectives contributed by different modules is a wrong answer with no symptom.\n\nUnknowns are unioned. If neither block declares a set, the result declares none either and keeps deriving its unknowns from its pairings, so composing does not freeze a block that was still derivable.\n\nThe result is never marked square, whatever its parts claimed. Two blocks can each be square alone and not compose to a square system — they may share a pairing, or one may supply constraints the other's unknowns do not cover — so inheriting the claim would skip the check exactly where it is most likely to catch something. assert_square! re-asserts and checks.\n\nA related change fell out of this: add_constraint! checked for a duplicate pairing by scanning every existing constraint, making block construction quadratic in its size. Blocks now carry a Set of claimed pairings, which matters once a model is assembled from modules.","title":"Composition"},{"category":"section","location":"design/#Naming-and-identifying-constraints","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C11.\n\nA paired constraint is named in the solver after the variable it determines. An unpaired one is named constraint[n] by its position among the solved constraints: a solver name has to be short and stable, so it cannot carry much.\n\nWhat actually identifies an unpaired constraint is where it was written. @block records a file:line on each entry it builds, and diagnose and a failed @check report it. That is the only handle an unpaired constraint has, since there is no variable name to call it by. The programmatic API records no source, and says so rather than inventing one.","title":"Naming and identifying constraints"},{"category":"section","location":"design/#Diagnosis-reports-rather-than-raises","page":"Design","text":"Decided 2026-09-14.\n\ndiagnose(block, dataset) returns the shape of the system — unknowns, equalities, inequalities, checks, objective, degrees of freedom, which solve path applies — together with three structural problems: constraints containing no unknown (a constant equation once exogenous values are substituted), unknowns appearing in no constraint, and exogenous variables with no value.\n\nIt reports rather than raises, because a diagnosis is something to read while building a model. The useful summary is the shape, not a square/not-square verdict, since degrees of freedom is defined on both paths.\n\nAn orphan means different things on each path, so the test differs: an unknown in no constraint is a bug in a square system, but legitimate if it appears in an objective, and a variable in the objective is therefore not reported.","title":"Diagnosis reports rather than raises"},{"category":"section","location":"design/#The-swap-interface","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C3.\n\nTwo primitives and one combination, all non-mutating:\n\nendogenize(block, items...) adds variables to the unknown set;\nexogenize(block, items...) removes them;\nswap(block, new => old, ...) does both and re-points the equation that determined old.\n\nNon-mutating because a calibration variant is then a value. SquareModels.jl's idiom is calibration = copy(block) followed by a mutating macro, and the copy is a step you can forget; here the original is untouched by construction.\n\nFunctions rather than a macro, and cells are selected with @group when they need selecting (swap(b, @group(mu[j in J]) => @group(L[j in J]))). That reuses the one index syntax the package already has instead of inventing a third, and keeps the macro count down.\n\nThey are endogenize/exogenize, not fix/free. JuMP exports fix and unfix, so taking those names would force every user of both packages to qualify the call — the same cost that ruled out shadowing @variables. These are also the words this literature already uses.\n\nThe primitive is membership of the unknown set, which means the same thing on both solve paths; re-pointing a pairing is the square-case extra, which is why swap is built on the primitives rather than beside them. exogenize raises on a variable some equation determines, pointing at swap: removing it silently would leave an equation determining something no longer being solved for.\n\nendogenize and exogenize clear the square claim, since they change the shape of the system. A swap preserves it — one pairing re-pointed, counts unmoved — so a block declared square stays declared square and is re-checked.","title":"The swap interface"},{"category":"section","location":"design/#Residuals-are-opt-in-and-paired-only","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C7.\n\nwith_residuals(block, datasets...) adds a residual variable to every paired solved constraint, added to the constraint function so x == rhs becomes x + residual(x) == rhs. Residuals are exogenous and zero, so they change nothing until asked to.\n\nNever created automatically, which is where SquareModels.jl differs: a residual doubles the variable count, and most sessions never use one. It is also meaningless for a constraint that determines nothing, so unpaired constraints get none.\n\nWhat they buy is locating inconsistent data, and it is expressed as a swap rather than as its own mechanism — exogenise the variable at its observed value, make its residual the unknown, and solve; the residual reads off how far the data misses the equation. That the debugging workflow falls out of the swap primitive rather than needing machinery of its own is the argument for having built the primitive first.\n\nApplying it twice is harmless: a constraint already carrying its residual is skipped, checked by looking for the residual in the constraint rather than by a flag that could drift.","title":"Residuals are opt-in and paired-only"},{"category":"section","location":"design/#Solve-settings-are-a-value","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C15.\n\nSolveOptions holds what had been nine keyword arguments retyped at every call site. Every field is still accepted as a keyword of solve! and overrides the options passed alongside it, so nothing that worked before means something different now.\n\nTwo lines fall out of having drawn it, and both are the useful part.\n\nSettings, not data. Starting values are a Dataset and stayed a keyword rather than becoming a field, because a SolveOptions that named a dataset would be tied to one model. As it stands one set of options is reusable across models and scenarios, which is what makes naming a set worth doing.\n\nThe optimizer became a per-solve setting rather than model state. It had been stored in model.ext, and passing optimizer = X to a solve attached X to the model as a side effect. A calibration and the baseline it feeds could not then use different solvers, or the same solver at different attributes, without mutating something shared. set_optimizer_factory! remains, as the model's default.\n\nThe solve strategy is not a setting, and briefly was. SolveOptions carried a strategy field until review pointed out it is the one field for which the type's own claim is false. Every other field is a knob that degrades gracefully on any model; a SolveStrategy selects an algorithm with preconditions — BlockTriangular raises on an objective, on a deficient system, on a solved inequality — so a named profile carrying one fails on some of the models it was meant to be reusable across, which is exactly what naming a profile is for. It is now a keyword of solve and a field of Problem. That sharpens both types rather than blurring one: SolveOptions is the model-independent settings, Problem is \"this block, this data, this way\".","title":"Solve settings are a value"},{"category":"section","location":"design/#The-pairing-is-also-a-matching","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C14.\n\nThe pairing was recorded above as documentation plus a precondition, and that stands. What was missed is that structurally it is also a perfect matching on the equation–variable incidence graph, which is the input to a Dulmage–Mendelsohn decomposition. The information was already there; decompose only reads it.\n\nOrienting the graph by the matching and running Tarjan gives the smallest subsystems that must be solved simultaneously, in an order where each one's inputs are already known. Where the pairings are missing or do not cover the unknowns, a maximum matching is computed instead and the coarse decomposition names the over- and under-determined parts.\n\nKept for diagnosis, and it pays for itself there. A count cannot distinguish a sound system from a structurally singular one. A block with two equations determining the same variable and a third variable determined by nothing has degrees_of_freedom == 0; decompose names all three. So diagnose now reports contested equations and undetermined unknowns, kept separate from orphans because \"appears in no equation at all\" and \"appears, but every equation that could determine it is determining something else\" are different bugs with different fixes. The decomposition costs 2–9 µs per unknown — about three orders of magnitude less than solving — so this is free.\n\nNot kept as a speed optimisation, and that was measured rather than assumed. BlockTriangular is 2× to 110× slower than solving monolithically, at every size where both work. The reason is specific: Ipopt's summed solve time over 400 one- and two-variable subsystems was 0.990 s against 0.004 s for the same system solved at once — 250× worse. An interior-point solver has a setup cost that is independent of problem size, and a scalar subsystem cannot amortise it. Building 400 JuMP models, at 1.83 ms each, is the smaller half of the overhead. The speedups reported for block-triangular form elsewhere assume each subsystem is solved by a direct method, not by re-entering a general-purpose NLP code.\n\nThe strategy is kept anyway, on a different claim: over 70 combinations of model shape and starting point, the monolithic solve converged 58 times and the block-triangular one 62, with five cases it alone solved against one it alone missed. Each subsystem starts from the results of the ones before it, where the monolithic solve gets whatever the user supplied for everything at once. So it is documented as a convergence fallback and not as an optimisation, which is the opposite of what was expected when it was written.\n\nThe gap does narrow with size — 12.3× at 2,150 unknowns, 1.8× at 21,500 — but no crossover was reached, because the block-triangular path became unreliable at 43,000 unknowns before the monolithic one did. Tables, and the arithmetic showing that a scalar fast path alone would not close the gap, are in archive/decompositionMeasurements.md.\n\nBlockTriangular refuses a block with a solved inequality, rather than solving it. An inequality determines nothing, so it belongs to no subsystem, so nothing would ever add it to a model — and the solve would return the answer to a different problem while reporting success. Review found this producing the wrong root of x² = 9 under x ≤ 0. Deciding which subsystem an inequality should ride along with has no correct answer in general, so refusing is the only honest option, and it is pinned by a test. The decomposition itself is right to ignore inequalities; the refusal belongs on the solve path.\n\nGraphs.jl is a dependency for this. Tarjan is short enough to write, and its recursive form overflows the stack on exactly the long chains this feature exists for; the established implementation is iterative. Call strongly_connected_components_tarjan and not the exported strongly_connected_components: the two are the same function today, but the exported name's docstring states that the order of the components is not part of its API contract, and that order is the entire basis of the solve order. Only the Tarjan name promises it.","title":"The pairing is also a matching"},{"category":"section","location":"design/#A-named-configuration,-and-what-it-is-allowed-to-promise","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C16.\n\nNothing in the package said which pairings of block and dataset were meaningful. solve(block, data) accepts all of them, so four blocks and five scenarios is twenty calls of which perhaps five mean anything, and the package had no opinion about which five.\n\nA Problem is one of the five: a block, the data it is solved against, where it starts and how it is solved. A ModelSpec is the set of them, in registration order.\n\nThe construction check is on the block, not on the data. A block is a value and cannot change underneath the problem, so a structural guarantee made about it holds. A Dataset is mutable by design — a scenario is a dataset that gets edited — so a data-dependent guarantee made at construction would be stale exactly when it mattered. Missing data is caught at solve time, where it is still exact. assert_solvable takes an optional dataset and draws the same line.\n\nThat split turned out to be worth more than the type. diagnose(spec) reports that :baseline is not ready to run because the parameter it needs is what :calibration produces — the ordering constraint of the whole workflow, visible without running anything and without anyone having declared it.\n\nWhich is why there is no dependency graph. An experiment inferred the edges from datasets being the same object, and examples/multiModeModel.jl showed why that cannot work here: solve is non-mutating and returns a fresh dataset, so the package's own idiom breaks the identity link the inference needs. The alternative, declaring the edges, was rejected for a better reason than difficulty: a declared graph is a second statement of the ordering that can drift from the equations, whereas readiness is derived from the actual state of the data and cannot. A declaration says what should be true; the readiness check says what is.\n\nNo accessors are exported for a Problem's fields. data and options are names a user will want for their own variables, and an exported function turns that assignment into an error rather than a shadow — the same cost recorded as notes/crossCuttingFindings.md #2. The fields are public and read directly.\n\nModes are values; there is no current mode. Nothing holds a \"the model is now in calibration state\" flag. That would cut against every other decision here — swap, endogenize and solve are all non-mutating — and it is the thing that makes a script's behaviour depend on lines that ran earlier somewhere else.","title":"A named configuration, and what it is allowed to promise"},{"category":"section","location":"design/#Soft-linking-needs-a-loop,-not-a-coupling-type","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C19.\n\nTwo models maintained separately, coupled by a few exchanged quantities and solved in alternation. The question was what structure this needs, and it was answered by writing one out by hand first (examples/softLink.jl) rather than by designing.\n\nCross-model transfer needs nothing. energy_data[q[s]] = macro_data[E[s]] is a read of one dataset and a write to another, and Dataset already refuses a variable belonging to the wrong model — so a mis-wired coupling raises instead of returning a plausible number. A Coupling type holding pairs of variables was considered and is not worth its own existence: it would buy nothing over an assignment, and it would have to grow a transform argument the moment a coupling aggregates or converts units, at which point a plain function is simpler than any signature.\n\nThe loop does need help, because of how it fails. Every individual solve in a diverging soft link converges and reports success — each model answers the question it was handed, and the pair never agrees. There is no symptom anywhere except that the exchanged quantities keep moving. A loop that runs out of iterations and returns its last values is therefore a wrong answer delivered quietly, which is the one failure mode this package is consistent about refusing. fixed_point! raises, with raise = false for the case where the history is what is wanted.\n\nThis is the same lesson as C18 one level up. There, a cascade handed each subsystem's answer to the next as exact and had no way to revisit it; here, a single pass hands one model's answer to the other and stopping there is the identical mistake. The convergence test is what makes the difference, so it is not optional.\n\nWhat is converged is the feedback, not everything transferred. exchanged lists the cells whose value at the start of a pass determines that pass. A quantity that a transfer overwrites before anything reads it carries no state between passes; damping it would be undone immediately, and including it in the convergence test measures something that was going to be overwritten anyway.\n\nConvergence is judged on the raw iterate, never on the damped blend. The first implementation blended and wrote back before measuring, which made the measured change (1 - damping) times the real one — so the effective tolerance was tol / (1 - damping), unbounded. Review found it reporting converged after one pass on a link with multiplier 1.00002 that grows without bound, and found accuracy silently degrading like 1 / (1 - damping) on links that do converge. That is precisely the false positive this function exists to prevent, arriving through the keyword meant to make it more reliable. The regression test uses a multiplier just above 1 on purpose: an obviously explosive link outruns the (1 - damping) shrink and hides the bug, which is why the original suite, whose only expansion case doubled each pass, missed it.\n\nTwo consequences worth keeping. A converging pass returns before blending, so the datasets hold values the models actually produced rather than a blend no solve ever saw. And the reported change is measured in multiples of the tolerance rather than in anyone's units, so one number covers cells of very different magnitude — with the raw magnitudes recorded separately as scale, because in tolerance units a geometrically diverging link looks flat: its tolerance grows with it.\n\nDamping fixes overshoot, not expansion, and the docstring says so because the distinction is not obvious and the failure is silent. If a pass multiplies the error by m, damping makes it (1 - damping)·m + damping. For m < -1 — the models leapfrogging, the change alternating in sign — some damping brings it inside (-1, 1). For m > 1 it does not, for any admissible value: a link that walks away in one direction is miscoupled rather than under-damped. Both cases are pinned by tests, the second precisely because a user will otherwise reach for damping and conclude the package is at fault.","title":"Soft-linking needs a loop, not a coupling type"},{"category":"section","location":"design/#Composing-modes-is-function-composition","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C20.\n\nThe registry holds a handful of named modes well. A model whose states are a product — static or dynamic closure, times calibration or baseline, times linked or unlinked — would need eight registrations repeating six ingredients between them, which is where a registry of finished Problems stops paying.\n\nIt needs nothing. A mode is a Block -> Block function and functions already compose, so the eight states are a loop over three axes with identity as each axis's \"leave it alone\" option. Measured on the example model: eight modes from six ingredients in five lines, with ready reporting which of them the data can support yet.\n\nA Mode type carrying a block transformation, a data transformation and an options overlay was considered. It would wrap ∘ in a name, and it would have to decide an application order that Julia's own composition already fixes. The registry stays a registry of Problems; what varies is how the Problem was built, which is the caller's business.\n\nTwo properties make the idiom safe enough to recommend, both checked rather than assumed. Where a swap and a + both apply they commute — the swap re-points one constraint, the sum unions the unknowns — so composing in either order gives the same block, constraint for constraint and pairing for pairing. And where they cannot commute, because the equation being re-pointed is in a block not yet added, swap raises and names the variable rather than silently building something adjacent.","title":"Composing modes is function composition"},{"category":"section","location":"design/#A-window-of-periods-is-a-composition,-not-a-restriction","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C21.\n\nSolving part of a horizon — periods 1 to 10 of fifty, one region of a multi-region model — needs the equations outside the window gone, not merely their variables exogenised: an equation left in with no unknown in it is a constant equation, true or false by accident of the data.\n\nNothing new is needed, provided the block is built as a composition in the first place. A block per period, and a window is compose(periods[a:b]). Everything outside is exogenous and read from the dataset by the ordinary substitution path, so a rolling horizon is a loop over windows sharing one dataset — each window reads the previous one's answer with no mechanism of its own, the same way a subsystem does in a block-triangular solve. Checked against solving the whole horizon at once: worst disagreement 2.4e-10 over twenty periods in five windows (archive/windowedSolve.jl).\n\nA window is a Block -> Block function, so it composes with the mode idiom above like any other axis.\n\nrestrict(block, group) — keeping the constraints that determine a given set of variables — was considered and deliberately not built. It would serve someone who already has a whole-horizon block written as one @block and wants to cut it down, which is a real position to be in. But it needs three judgement calls with no obviously right answer: what to do with a solved constraint that is unpaired and so cannot be attributed to any variable, what to do with @check constraints written over the whole horizon, and whether a dropped equation should be an error or a silent omission. Reopen it when a model exists that cannot reasonably be written per period — not before, because the composition route has none of those questions.","title":"A window of periods is a composition, not a restriction"},{"category":"section","location":"design/#Combining-many-blocks-is-one-pass,-not-a-fold","page":"Design","text":"Decided 2026-09-14 — notes/TODO.md C22.\n\n+ is non-mutating, which is right: a composed block is a value and neither part is disturbed. The cost is that it copies both constraint vectors every time, so folding it over n blocks copies O(n²) constraints. Measured at 0.32 s for 2,000 two-equation blocks, against 1.25 ms for a single pass — and building a model out of many small blocks, one per period or per sector, is precisely the idiom this package exists to support, so the quadratic path was on the main road rather than off it.\n\ncompose(blocks) applies the same rules in one pass, and sum over a Vector or Tuple of blocks routes to it. sum over a generator cannot be intercepted without claiming a method far too broad, so it still folds; compose's docstring says so rather than leaving it to be discovered.\n\nThis is the second time non-mutating composition has cost something quadratic — add_constraint! scanned every existing constraint for a duplicate pairing until C6 replaced it with a Set. The pattern is worth naming: an operation that is cheap for two and used for two thousand.","title":"Combining many blocks is one pass, not a fold"},{"category":"section","location":"design/#A-declaration-shorthand,-under-a-name-JuMP-does-not-own","page":"Design","text":"Decided 2026-09-15 — notes/TODO.md C23.\n\n@declare model begin ... end is JuMP.@variables with a description as the last positional entry of a row, and tags after :: applying to every variable the block declares.\n\nThis does not reopen C8. Two things were conflated there and are separable: what metadata is attached, and where it is written. C8 settled the first — metadata is keyed on VariableRef, attached by function, never at a shadowed @variables — and this changes none of it, because the macro expands to exactly the describe! and tag! calls a user would write by hand. What it changes is the second, and only as sugar: the rows go to JuMP.@variables untouched and the return value is JuMP's own tuple, so there is no second declaration mechanism to keep in step, and a model written with @variable behaves identically.\n\nThe name is the whole reason this is a decision rather than a patch. @variables is a JuMP export, so taking it would force every user of both packages to qualify — the same cost that ruled out fix. The description slot is safe to claim because JuMP rejects a bare positional string today (\"Unrecognized positional arguments\"), which is a slot it cannot later want.\n\nDescriptions attach by tuple position, not by parsing the name out of the row. JuMP.@variables returns one element per row in row order, so the container is already in hand: there is nothing to guess about which side of 0 <= x <= 1 is the variable, and anonymous rows — which have no name at all — get their description and tags like any other. A parser over declaration heads would have been a second, weaker copy of JuMP's own.\n\nTags are block-level only. Per-row tags are a tag! line, on the grounds that the exported surface is the product: :: inside a row would be a third place where a tag can be written, buying a line.\n\nTwo hygiene traps are recorded in the source because both are silent and neither is guessable: the macro hands JuMP.@variables a gensym escaped into the caller's scope, since that macro escapes its own arguments; and the whole nested macrocall is escaped, because hygiene otherwise descends into its arguments and resolves the user's index sets in ModularSystems — J became ModularSystems.J.","title":"A declaration shorthand, under a name JuMP does not own"},{"category":"section","location":"design/#Open","page":"Design","text":"Whether slices return views (C12) and a sparse notation layer (C13) are both deferred deliberately rather than forgotten, each with the thing that would reopen it. C12 waits for a measurement showing that slice reads or writes are actually hot — Window, prepare_selection and a model-layout cache are a large surface to add on a guess. C13 waits for two or three real models: it is a pure ergonomics question, whether x[p, i, t] with gaps returning zero is worth a wrapper array when x[k, t] over an IndexSet already works.","title":"Open"},{"category":"section","location":"#ModularSystems.jl","page":"Home","text":"Modular framework for building, calibrating and solving economic models in Julia.\n\nModels are assembled from composable blocks of constraints. Each constraint may be paired with the variable it determines — square systems get a dedicated solver — but a block may equally carry an objective and be solved as an optimization problem.\n\nwarning: Early days\nEverything documented here works, but nothing in the API is stable. See Design for what has been decided and why.","title":"ModularSystems.jl"},{"category":"section","location":"#Installation","page":"Home","text":"julia> using Pkg; Pkg.develop(url = \"https://github.com/ChampionApe/ModularSystems.jl\")\n\nYou also need a solver. Anything JuMP supports will do; the examples here use Ipopt. (Ipopt prints a licence banner from its C library on the first solve, before any verbosity setting applies — the sb option below is what suppresses it.)","title":"Installation"},{"category":"section","location":"#A-first-model","page":"Home","text":"Two labour types, each with a productivity and a workforce. Each equation is written next to the variable it determines.\n\njulia> using ModularSystems, JuMP, Ipopt\n\njulia> model = Model();\n\njulia> set_optimizer_factory!(model, optimizer_with_attributes(Ipopt.Optimizer, \"sb\" => \"yes\"));\n\njulia> J = 1:2;\n\njulia> L, w, N, rho = @declare model begin\n           L[J],   \"Labour demand\"\n           w[J],   \"Wage\"\n           N[J],   \"Workforce (exogenous)\"\n           rho[J], \"Productivity (calibrated)\"\n       end;\n\njulia> block = @block model begin\n           @square\n           L[j in J], L[j] == rho[j] * N[j]\n           w[j in J], w[j] == L[j] / 100\n       end;\n\njulia> issquare(block)\ntrue\n\njulia> degrees_of_freedom(block)\n0\n\n@declare is JuMP.@variables with a description column: the rows are handed to JuMP unchanged and it returns the same tuple of containers, so @variable and JuMP.@variables remain equally valid ways to declare. Descriptions are optional per row, and a row without one is an ordinary JuMP declaration.\n\n@square is an assertion: the block claims to be square and is checked as it is built. Drop it and nothing is checked — squareness is a property here, not a requirement.\n\nValues live in a Dataset, one per scenario:\n\njulia> data = Dataset(model);\n\njulia> data[N] = [3200.0, 500.0];\n\njulia> data[rho] = [1.0, 2.0];\n\njulia> baseline = solve(block, data; replace_nothing = 1.0);\n\njulia> round(baseline[L[1]], digits = 2)\n3200.0\n\njulia> round(baseline[w[2]], digits = 2)\n10.0\n\nsolve returns a new dataset and leaves data alone; solve! writes in place. Anything the block does not solve for — here N and rho — is substituted from the dataset as a number and never reaches the solver.\n\nA scenario is a copy with something changed, and scenarios compare with ordinary arithmetic:\n\njulia> scenario = copy(baseline);\n\njulia> scenario[N] = [2700.0, 1000.0];\n\njulia> scenario = solve(block, scenario; replace_nothing = 1.0);\n\njulia> multipliers = scenario ./ baseline .- 1;\n\njulia> round(multipliers[L[2]], digits = 4)\n1.0\n\nArithmetic works on values and returns a value-only dataset: bounds and solve metadata are dropped, because a ratio of two scenarios is not itself a scenario.","title":"A first model"},{"category":"section","location":"#Solve-settings","page":"Home","text":"replace_nothing = 1.0 above is one of a handful of settings a solve takes. Retyping them at every call site is how two solves end up differing by a keyword nobody meant to change, so they can be named as a value:\n\njulia> opts = SolveOptions(replace_nothing = 1.0)\nSolveOptions(replace_nothing = 1.0)\n\njulia> round(solve(block, data; options = opts)[L[1]], digits = 2)\n3200.0\n\nPrinting shows only what differs from the defaults, which is the part worth reading. Derive a variant rather than rebuilding one, so the two cannot drift apart:\n\njulia> loud = SolveOptions(opts; silent = false);\n\njulia> (loud.replace_nothing, loud.silent)\n(1.0, false)\n\nEvery field of SolveOptions is also accepted directly as a keyword of solve, where it overrides the options passed alongside it — so solve(block, data; options = opts, silent = false) and the loud above mean the same thing.\n\nSolveOptions holds nothing belonging to a particular model, which is what makes one set reusable across models and scenarios. Starting values are a Dataset and therefore data, so they stay a keyword of their own:\n\njulia> solved = solve(block, data; options = opts, start_values = baseline);\n\njulia> round(solved[w[1]], digits = 2)\n32.0\n\nTwo solvers, two scopes. set_optimizer_factory! sets a model's default, as in the quickstart. optimizer inside SolveOptions names the solver for that solve only and does not attach itself to the model — which is what lets a calibration use different solver settings from the baseline it feeds.\n\nOne thing is deliberately not a setting. Every field above is a knob that means the same thing on any model, which is what makes a named set reusable. The solve strategy selects an algorithm with preconditions, so a profile carrying one would fail on some of the models it was meant to be reused across. It is a keyword of solve and a field of Problem instead.","title":"Solve settings"},{"category":"section","location":"#When-the-system-is-not-square","page":"Home","text":"A block may instead carry an objective, and is then solved as an optimization problem. Nothing else changes: the same macro, the same dataset, the same substitution of exogenous values.\n\njulia> @variable(model, Lhat[J]);   # observed labour demand\n\njulia> @variable(model, rho_bar);   # one productivity shared by both types\n\njulia> estimation = @block model begin\n           @unknowns rho_bar, L\n           [j in J], L[j] == rho_bar * N[j]\n           @objective Min sum((L[j] - Lhat[j])^2 for j in J)\n       end;\n\njulia> issquare(estimation)\nfalse\n\njulia> degrees_of_freedom(estimation)\n1\n\njulia> obs = Dataset(model);\n\njulia> obs[N] = [100.0, 200.0];\n\njulia> obs[Lhat] = [110.0, 190.0];\n\njulia> fitted = solve(estimation, obs; replace_nothing = 1.0);\n\njulia> round(fitted[rho_bar], digits = 4)\n0.98\n\nTwo unknowns, one equation each, and an objective — so one degree of freedom, and least squares picks the point. issquare is false and nothing complains, because squareness is a property of a block rather than a requirement of one.\n\nPairings are still allowed on a block with an objective; they are simply inert, since the pairing is documentation and a square-solver precondition rather than something that orders the system.","title":"When the system is not square"},{"category":"section","location":"#Calibration-is-a-swap","page":"Home","text":"Calibration solves the same equations for different variables: a parameter becomes unknown, an observed outcome becomes data. swap exchanges them and re-points the equation that determined the outcome, leaving the equations themselves untouched.\n\njulia> calibration = swap(block, rho => L);\n\njulia> collect(unknowns(calibration))[1] == rho[1]\ntrue\n\njulia> issquare(calibration)\ntrue\n\njulia> observed = Dataset(model);\n\njulia> observed[N] = [3200.0, 500.0];\n\njulia> observed[L] = [3200.0, 1000.0];\n\njulia> calibrated = solve(calibration, observed; replace_nothing = 1.0);\n\njulia> round(calibrated[rho[1]], digits = 4)\n1.0\n\njulia> round(calibrated[rho[2]], digits = 4)\n2.0\n\nThe behavioural block, unchanged, now reproduces the observation from those parameters — and a swap preserves the shape of the system, so a block declared square stays declared square and is re-checked.\n\nEither side may be a variable, a container or a group, and group sides pair in iteration order — which is why a VariableGroup is ordered. Use @group when the cells need selecting:\n\ncalibration = swap(block, @group(rho[j in J; j == 1]) => @group(L[j in J; j == 1]))\n\nUnderneath are two primitives that mean the same thing whether or not the block is square: endogenize adds variables to the unknown set, exogenize removes them. swap is the square-case combination that also re-points the pairing. Exogenising a variable that an equation determines raises and points you at swap, since it would otherwise leave an equation determining something no longer being solved for.\n\n(They are endogenize/exogenize rather than fix/free because JuMP exports fix and unfix; taking those names would force every user of both packages to qualify the call.)","title":"Calibration is a swap"},{"category":"section","location":"#Composing-blocks","page":"Home","text":"A block is a unit of model code, and a model is their sum. Each module owns its equations:\n\njulia> labour = @block model begin\n           L[j in J], L[j] == rho[j] * N[j]\n       end;\n\njulia> wages = @block model begin\n           w[j in J], w[j] == L[j] / 100\n       end;\n\njulia> whole = labour + wages;\n\njulia> length(whole)\n4\n\njulia> issquare(whole)\ntrue\n\nA variable determined in both blocks raises rather than one equation quietly winning, and at most one objective may appear across a sum.\n\nFor many blocks — one per period, one per sector, which is the idiom this package is for — use compose, which combines them in a single pass:\n\njulia> length(compose([labour, wages]))\n4\n\n+ is non-mutating, so it copies both constraint vectors each time and folding it over n blocks copies O(n²) constraints: 0.32 s for 2,000 two-equation blocks, against about a millisecond for compose. sum over a Vector or Tuple of blocks calls compose for you; sum over a generator cannot, and falls back to the quadratic fold.\n\nBuilding a model this way — one block per period, or per sector — buys something beyond tidiness: a window is then a composition too. Given periods = [period(t) for t in 1:50], compose(periods[1:10]) is the first decade and nothing else, with every later period exogenous and read from the dataset by the ordinary substitution path. A rolling horizon is a loop over windows sharing one dataset, each reading the previous one's answer with no mechanism of its own:\n\nfor start in 1:10:50\n    solve!(compose(periods[start:(start + 9)]), data; options = opts)\nend\n\nSolving the whole horizon at once and rolling it in five windows agree to 2.4e-10 on the model in archive/windowedSolve.jl. Note that exogenising the later variables would not be enough on its own — their equations have to be gone, since an equation left in with no unknown left in it is a constant equation, true or false by accident of the data.\n\nA composed block is never marked square, whatever its parts claimed — two square blocks need not compose to a square system, so inheriting the claim would skip the check where it is most likely to catch something. Re-assert explicitly:\n\njulia> whole = assert_square!(whole);\n\njulia> issquare(whole)\ntrue","title":"Composing blocks"},{"category":"section","location":"#Bounds","page":"Home","text":"Bounds intrinsic to a variable — K >= 0 — belong on the JuMP variable. Bounds that belong to this problem go in the dataset, where they can differ between scenarios:\n\njulia> set_bounds!(data, L[1]; lower = 0.0);\n\njulia> bounds(data, L[1])\n(0.0, nothing)\n\nThe bound applied at solve time is the intersection of the two, and an empty intersection raises rather than surfacing later as an unexplained infeasibility.\n\nOn a square solve, a bound that is active at the solution raises BindingBoundError: the system did not determine the answer, and the solver would have reported success anyway. On an optimization solve an active bound is expected, so it is recorded in meta.binding_bounds instead.","title":"Bounds"},{"category":"section","location":"#Blocks,-pairings-and-squareness","page":"Home","text":"A block is a composable collection of constraints over a JuMP model, together with the set of variables being solved for and, optionally, an objective. Blocks are developed, tested and documented one at a time, then summed into the system you actually solve.\n\nEach constraint may be paired with the variable it is understood to determine. The pairing is documentation and a precondition: it names constraints in solver output and diagnostics, and it is what the square solver checks before it runs. It does not order the system or drive the solution write-back, and in a block with an objective it is inert.\n\nA system is square when it has no objective and every solved constraint is an equality paired with a distinct variable, together covering the unknowns. That is the common case in macroeconomic modelling and it gets a dedicated solve path. It is not a requirement — the number that generalises is the degrees of freedom, which is defined either way.\n\nCalibration is then not a special mode: it is the same block with a different partition of variables into unknown and fixed.","title":"Blocks, pairings and squareness"},{"category":"section","location":"#Relation-to-SquareModels.jl","page":"Home","text":"Very similar to SquareModels.jl at its core, with different approaches to inequalities, variable bounds and a number of other things. An independent implementation: no shared code, not a fork, and no dependency on it.","title":"Relation to SquareModels.jl"},{"category":"section","location":"#Diagnosing-a-block","page":"Home","text":"diagnose reports the shape of a system and its structural problems without running a solver. It reports rather than raises: it is meant to be read while a model is being written.\n\njulia> report = diagnose(whole, data);\n\njulia> isclean(report)\ntrue\n\njulia> report.degrees_of_freedom\n0\n\njulia> report.square\ntrue\n\nIt names five things that would otherwise surface as an unexplained solver failure. Three are found by looking at each constraint in turn: constraints left with no unknown once exogenous values are substituted (trivial), unknowns appearing in no constraint and no objective (orphans), and exogenous variables with no value (missing_values). A @block entry records where it was written, so an unpaired constraint — which has no variable name to be called by — is still identifiable.\n\nThe other two need the decomposition below, and are the ones a count cannot find. Here two equations determine the same variable and another unknown is left with nothing:\n\njulia> @variable(model, lonely);\n\njulia> duplicated = @block model begin\n           @unknowns L, lonely\n           L[j in J],  L[j] == rho[j] * N[j]\n           [i in 1:1], 2 * L[1] == 2 * rho[1] * N[1]\n       end;\n\njulia> bad = diagnose(duplicated, data);\n\njulia> bad.degrees_of_freedom      # three unknowns, three equations\n0\n\njulia> isclean(bad)\nfalse\n\njulia> [i for (i, _) in bad.contested]\n2-element Vector{Int64}:\n 1\n 3\n\ncontested lists equations left with no variable to determine, because everything they could have determined is already spoken for. undetermined is its mirror: unknowns that do appear in equations, but where every equation that could have determined them is determining something else.\n\nundetermined is kept separate from orphans because they are different bugs. An orphan appears in no equation at all, which is almost always a typo. An undetermined unknown appears in equations that have been taken by something else, which is a modelling error — and the fix is a different one.\n\nThe counts say nothing is wrong here: three unknowns, three equations, zero degrees of freedom. Only the structure shows that one equation was written twice and lonely is determined by nothing.","title":"Diagnosing a block"},{"category":"section","location":"#Decomposition","page":"Home","text":"A square block pairs each equation with the variable it determines. Read as a graph, that pairing is a perfect matching, and a matching is what a system needs to be split into the smallest groups of equations that must be solved together:\n\njulia> dec = decompose(whole)\nDecomposition(4 subsystems, largest 1)\n\njulia> iswelldetermined(dec)\ntrue\n\njulia> largest_subsystem(dec)\n1\n\nsubsystems come back in solve order, whatever order the equations were written in: each one's inputs are determined by the ones before it. largest_subsystem is the size of the biggest group that has to be solved simultaneously — 1 here, because nothing in whole is mutually determined.\n\nWhere the pairings are missing or do not cover the unknowns, a maximum matching is computed instead, and overdetermined and underdetermined name the parts that are left over. That is where contested and undetermined above come from.\n\ndecompose is cheap — a few microseconds per unknown, about a thousandth of the cost of solving — so it is meant to be used freely while a model is being built.","title":"Decomposition"},{"category":"section","location":"#Solving-one-subsystem-at-a-time","page":"Home","text":"BlockTriangular solves each subsystem in turn, each reading the results of the ones before it out of the dataset:\n\njulia> round(solve(block, data; options = opts, strategy = BlockTriangular())[L[1]], digits = 2)\n3200.0\n\nwarning: It is slower, and that is measured\nThis is not a speed optimisation. It is 2× to 110× slower than the default Monolithic across model sizes up to 21,500 unknowns. Entering an interior-point solver costs about the same whatever the problem size, and a one- or two-variable subsystem cannot amortise that: Ipopt's summed time over 400 tiny subsystems was 0.99 s against 0.004 s for the same system solved at once.What it buys is convergence on a short system. Every subsystem starts from the results of the ones before it, where a monolithic solve starts from whatever was supplied for everything at once.But each subsystem is solved to an absolute tolerance and its answer is handed on as exact, with nothing downstream able to correct it. On a long recursive chain that error accumulates — measured 2,700× worse than monolithic over 700 periods, and outright failure at 1,000. Reach for it on a short system that will not converge; not on a long recursive one, and never for speed.\n\nIt refuses rather than guessing: a block with an objective, one that is over- or under-determined, or one carrying a solved inequality — which determines nothing, so it belongs to no subsystem — raises instead of quietly solving a different problem.","title":"Solving one subsystem at a time"},{"category":"section","location":"#Problems-and-modes","page":"Home","text":"A model is never solved one way. It is calibrated, run as a baseline, shocked; its satellite modules are sometimes linked in and sometimes held exogenous. Each of those is a block, a dataset and some settings — and solve(block, data) will accept any pairing of them, meaningful or not.\n\nA Problem is one pairing that means something:\n\njulia> p = Problem(whole, data; options = opts)\nProblem(4 constraints, 4 unknowns)\n\njulia> round(solve(p)[w[2]], digits = 2)\n10.0\n\nA shock is a baseline with something different about it, so derive rather than rebuild:\n\nshock = Problem(baseline_problem; data = shocked, start = baseline)\n\nThe constructor checks the block — see assert_solvable — and deliberately not the data. A block is a value and cannot change underneath the problem, so a guarantee about it holds. A Dataset is mutable by design, since a scenario is a dataset that gets edited, so a check on the data taken at construction would be stale exactly when it mattered. Missing data is caught at solve time instead, where it is still exact.\n\nA ModelSpec collects the named modes:\n\njulia> spec = ModelSpec(model);\n\njulia> register!(spec, :baseline, p; about = \"forward run\");\n\njulia> modes(spec)\n1-element Vector{Symbol}:\n :baseline\n\njulia> ready(spec)\n1-element Vector{Symbol}:\n :baseline\n\nready is how a workflow's ordering is expressed here, and it is derived, not declared: a calibration comes before the baseline that consumes its output because the baseline's parameters are not in the data until the calibration has run. Nobody writes that down, so nobody can write it down wrongly. diagnose(spec) says why a mode is not ready, and displaying a spec shows it per mode.\n\nThere is deliberately no dependency graph and no \"current mode\". Modes are values; the one you are working with is a variable in your own code, not hidden state in the model.\n\nexamples/multiModeModel.jl runs the whole story end to end — two calibrations, linked and unlinked baselines, and a shock — on a two-sector model with an energy satellite.","title":"Problems and modes"},{"category":"section","location":"#Modes-over-several-axes","page":"Home","text":"A registry suits a handful of named modes. Real models often have a product instead: static or dynamic closure, times calibration or baseline, times linked or unlinked — eight states from six ingredients, and writing eight assemblies by hand repeats every ingredient several times.\n\nThis needs nothing from the package. A mode is a Block -> Block function, and functions already compose:\n\njulia> add_wages(b) = b + wages;\n\njulia> to_calibration(b) = swap(b, rho => L);\n\njulia> for stage in (identity, to_calibration), extra in (identity, add_wages)\n           b = (stage ∘ extra)(labour)\n           println(length(b), \" constraints, \", length(unknowns(b)), \" unknowns\")\n       end\n2 constraints, 2 unknowns\n4 constraints, 4 unknowns\n2 constraints, 2 unknowns\n4 constraints, 4 unknowns\n\nidentity is the \"leave this axis alone\" option, so every axis has the same shape. Registering the whole product is then a loop over the axes, and the readiness column makes the resulting matrix legible — which modes are runnable now, and which are waiting on a calibration.\n\nTwo things make this safer than it looks. Where a swap and a + both apply, they commute: the swap re-points one constraint and the sum unions the unknowns, so composing in either order gives the same block, constraint for constraint and pairing for pairing. And where they cannot commute — calibrating against an equation that the blocks composed so far do not contain — swap raises and names the variable, rather than building something subtly different.","title":"Modes over several axes"},{"category":"section","location":"#Soft-linking-separate-models","page":"Home","text":"Sometimes two models cannot be made one. A macro model and a bottom-up energy model are maintained by different people at different granularity, and neither can be pasted into the other — so they are coupled by a handful of quantities instead, solved in alternation until neither changes what it hands over.\n\nAlmost none of that needs the package. Passing a value across is a read of one dataset and a write to another:\n\nenergy_data[q[s]] = macro_data[E[s]]\n\nand a Dataset refuses a variable belonging to the wrong model, so a mis-wired coupling raises rather than producing a plausible number. The models themselves are ordinary Problems.\n\nWhat does need help is the loop, and only because of how it fails. Every individual solve in a diverging soft link converges and reports success — each model answers the question it was given perfectly well, and the pair never agrees. There is no other symptom, so a loop that runs out of iterations and returns its last values hands back numbers that look exactly like a solution. fixed_point! raises instead:\n\nreport = fixed_point!([macro_data => pe]) do\n    solve!(macro_problem)\n    for s in S\n        energy_data[q[s]] = macro_data[E[s]]\n    end\n    solve!(energy_problem)\n    macro_data[pe] = energy_data[price]\nend\n\nThe list is the cells that carry information between passes — the feedback, whose value at the start of a pass determines that pass. q[s] is transferred too, but a transfer overwrites it before anything reads it, so it carries no state and is not what the loop is converging.\n\ndamping moves each exchanged cell only part of the way to its new value, which is what settles two models that overshoot each other. It fixes overshoot and not expansion: if the change alternates in sign, damping will help; if the link walks away in one direction, no admissible amount of damping brings it back and the coupling itself is wrong. A report carries scale — the size of the exchanged values at each pass — which is what tells those two apart.\n\nConvergence is judged on the raw values the models produced, never on the damped blend. Judging the blend would scale the measured change by (1 - textdamping) and so scale the effective tolerance by 1(1 - textdamping), which is unbounded: a heavily damped run would report convergence on a link that never settles, which is the one thing this function exists to prevent.\n\nchange and history are in multiples of the tolerance, so 1.0 is exactly at tolerance and one number is comparable across cells of very different magnitude. scale is separate because history alone cannot distinguish divergence from stagnation — a link growing geometrically has a roughly constant relative change, since the tolerance grows with the value.\n\nexamples/softLink.jl shows a link that settles, the same link made steep enough that it will not, and damping fixing it.","title":"Soft-linking separate models"},{"category":"section","location":"#When-a-solve-raises","page":"Home","text":"Five failures have names, because each means something different and calls for a different fix.\n\nraised by means\nStructuralError the block cannot be solved whatever the data says. Carries the Diagnosis that found it.\nBindingBoundError a bound is active at the solution of a square system, so the equations did not determine the answer. Never raised on the optimization path, where an active bound is expected.\nCheckFailure a @check constraint did not hold at the solution. Carries each failing check and the gap it missed by.\nSubSystemFailure a subsystem failed during a BlockTriangular solve. Names which one and what it was solving for; everything before it succeeded, so the dataset holds those results.\nConvergenceFailure soft-linked models did not settle. Carries the history, which says whether they were diverging or merely slow.\nArgumentError the request itself does not make sense — an exogenous variable with no value, an empty bound interval, a block declared square that is not.\n\nThe first four are the package refusing to hand back a plausible wrong answer. That is the thing it is most careful about: a solver reporting success on a problem that is not the one you asked about is worse than an interruption.","title":"When a solve raises"},{"category":"section","location":"#Tags-and-descriptions","page":"Home","text":"Tags are cross-cutting labels on variable cells, attached after declaration and turned back into a group on demand. Nothing shadows JuMP.@variables, so every JuMP declaration form keeps working.\n\njulia> const Quantity = Tag(:quantity);\n\njulia> tag!(model, Quantity, L);\n\njulia> length(tagged(model, Quantity))\n2\n\njulia> describe!(model, w, \"Wage by labour type\");\n\njulia> description(w[1])\n\"Wage by labour type\"","title":"Tags and descriptions"},{"category":"section","location":"#Attaching-both-at-declaration","page":"Home","text":"@declare is the shorthand for doing this while the variables are being written down: a description per row, and tags after :: that apply to everything the block declares, anonymous rows included.\n\njulia> using ModularSystems, JuMP\n\njulia> const Quantity = Tag(:quantity);\n\njulia> m = Model(); T = 2020:2022;\n\njulia> qGDP, qC = @declare m::Quantity begin\n           qGDP[t in T], \"Real GDP\"\n           qC[t in T] >= 0, (start = 1.0), \"Real consumption\"\n       end;\n\njulia> description(qGDP[2021])\n\"Real GDP\"\n\njulia> length(tagged(m, Quantity))\n6\n\njulia> lower_bound(qC[2020]), start_value(qC[2020])\n(0.0, 1.0)\n\nBounds, start and every other JuMP argument pass straight through — the description is the last positional entry in the row and the only part @declare reads. Per-variable tags are tag! on a following line; nothing is lost by declaring with @variable instead and attaching both afterwards, which is what @declare expands to.","title":"Attaching both at declaration"},{"category":"section","location":"#Sparse-patterns","page":"Home","text":"A sparse variable declared with a filter makes JuMP evaluate the predicate over the full product of its axes, which scales with that product rather than with the number of variables created. An IndexSet is the pattern as a value, and declaring over it is plain JuMP with no scan:\n\njulia> pairs = IndexSet((:p, :i), [(:food, :agri), (:steel, :mfg), (:food, :mfg)]);\n\njulia> @variable(model, use[pairs]);\n\njulia> length(pairs)\n3\n\njulia> collect(select_axes(pairs, :p))\n2-element Vector{Tuple{Symbol}}:\n (:food,)\n (:steel,)\n\nA coordinate-keyed container is dense over its own axes, so there are no absent cells to guard. Equations iterate the pattern instead of ranging over a product, and group_by is what replaces summing over one index of a sparse variable:\n\njulia> by_product = group_by(pairs, :p);\n\njulia> length(by_product[(:food,)])\n2\n\nIndexSet carries the same ∪, ∩ and setdiff as VariableGroup — one is a set of coordinates, the other a set of the cells at those coordinates.","title":"Sparse patterns"},{"category":"section","location":"#Reference:-the-@block-grammar","page":"Home","text":"Form Meaning\nx, expr constraint paired with x\nx[i in I], expr indexed, paired with x[i]\nx[t0], expr a fixed index — pairs with that one cell, no loop\n[i in I], expr indexed, unpaired\nexpr scalar, unpaired\n@check expr \"msg\" evaluated after a solve, never solved\n@unknowns ... declares the unknown set\n@objective Min expr attaches an objective\n@square asserts squareness, checked as the block is built\n\nA comma-separated head names the variable a constraint determines; brackets alone give index sets and leave the constraint unpaired. A filter goes after ;, as in x[t in T; t > t0]. Fixed and looped positions may be mixed: x[s in S, :Equity, t in T].\n\n@unknowns is required once any solved constraint is unpaired. That is what turns a dropped variable name — [i in I], ... where x[i in I], ... was meant — into a degrees-of-freedom mismatch rather than a silent change of problem.\n\nA pairing on an inequality is an error, since an inequality determines nothing.","title":"Reference: the @block grammar"}]
}
